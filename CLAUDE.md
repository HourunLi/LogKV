# SemanticLogKV 开发指导文档（存档，随讨论推进持续更新）

> 本文件是 `semanticLogKV` 分支的唯一存档文档，记录"语义簇 + 簇级抽象位置"这条压缩
> 方案的设计、实现进度、实验结果和踩过的坑。**每次讨论出现突破或进展，都要写回本
> 文件**（新增到 §11 变更记录，并同步更新受影响的正文章节），不要另开文档。
>
> 本文件只保留 SemanticLogKV 直接相关的内容。原 `logKV`/`claude/semantic-cluster-
> log-compression-8ai32a` 分支上的 CLAUDE.md 是另一条独立技术路线（位置分桶 +
> rank-1 二阶修正 + pin 机制）的进展存档，与本分支无关，不在这里维护——那条线的
> 结论只在本文档 §1、§9 里作为背景锚点和教训引用，不展开叙述。

## 0. 现状速览（2026-08-14）

**这是什么**：LogKV 现有压缩机制（`litgpt/log_kv_cache.py`）按**位置**做 Fenwick 树式
分层压缩——固定 2:1 合并，越老的 token 槽越宽、压得越狠。已验证的结论（另一分支的
存档）：这套方案的最大瓶颈是**槽内异质性**——语义无关的 token 被硬塞进同一个位置槽
做均值池化，needle 稀释成 1/width，niah@32768 从稠密上限 0.9353 掉到 0.0827。

**本分支的方案**：把"按位置分桶"换成"按语义分簇"——语义相近的 token 才共享槽，
needle 因为和周围内容语义不相关而倾向于自成一簇，槽内不再异质，稀释问题从结构上
消失。配套需要解决两个新问题：①位置信息不能再靠 post-RoPE 均值池化表示；②簇数
不能固定，需要由内容驱动、预算仍是次线性的自适应机制。

### 设计迭代史（三轮，v3 是当前版本）

| 版本 | 位置表示 | 为什么被取代 |
|---|---|---|
| v1 | 逐频率复数统计量 `z`（特征函数）+ 标量 `β` 插值 gain | β 把"往哪转"和"多可信"耦合进一个标量，且 β=0 时对最不可信的频率通道施加最大重物化力度，方向性错误 |
| v2 | `z` + 恒定满模长旋转 + 独立的 `κ·log ρ` 置信项 | 修好了"不确定性污染内容"，但 **z 本身仍是加权和，相位相消依然发生**，只是被诚实报告出来而已 |
| **v3（当前）** | **真实锚点位置集 `(p_lo, p_hi, p_mid)` + ddCRP 时序门控** | — |

**v3 的核心转变：不再试图"把多个位置压成一个数"，而是不做任何平均。** 每个 entry 存
它覆盖范围内**真实成员**的边界与集中锚点，读出时在这些真实位置上各做一次标准
`apply_rope`。每个虚拟槽的 `ρ` 恒为 1，**相位相消从数学上根本不会发生**——不是被
压制，是压根没有"求平均"这一步给它可乘之机。完整推导见 §2.2/§2.3。

配套的第二项转变：路由从纯语义 DP-means 改为 **ddCRP（距离依赖 CRP）**——语义相似
但时序上已被其它簇打断的，强制 fork 新簇。这不是补丁，是让锚点表示能成立的前提，
见 §2.1 里那个 60% 的实证数字。

**当前阶段：设计已完成三轮迭代，代码尚未开始写。**

**下一步（按优先级）**：
1. Stage 0 离线证伪实验（§6）——不需要 GPU 训练，只需要一次 dump + CPU 分析，
   决定整个方案值不值得往下做。**这是唯一应该先做的事**，不要跳过直接写生产代码。
   v3 新增两项决定性测量：S0.6（锚点去重倍数 → 内存是否失控）、S0.7（簇内 supersession
   频率 → §2.4 那个开放问题的判定实验）。
2. `litgpt/log_kv_position.py` 纯函数 + 单测（§5.1），CPU 可测，不依赖 dump 结果。
3. 视 Stage 0 结果决定是否继续 Stage 1（生产代码）。

## 1. 背景：为什么从位置分桶转向语义分簇

（完整证据链在另一分支的 CLAUDE.md 里，这里只摘录作为本方案设计依据的结论性数字，
不重复过程。）

- 稠密 attention 上限（CPT 后模型）：niah@32768 = **0.9353**。
- 现有 LogKV（位置分桶 + rank-1 二阶修正，无 pin）：niah = **0.0827**，LongBench =
  **0.1716**，LongBench_e = **0.1918**，ACC = **0.6113**。压缩本身吃掉 85+ 个百分点，
  是全链路最大的单一瓶颈，比 pin 全系列实验的影响大一个数量级。
- 机制诊断（D1 自检）：width≥8 时 rank-1 近似失真明显；根因是均值池化把 needle
  稀释成 1/width，二阶修正是 rank-1、只能加不能减，救不回一个已经被稀释到"不参与
  softmax 竞争"的槽,尤其救不回**读出侧**（`v_slot` 同样被稀释,Γ 修正同样是 rank-1）。
- 已经试过的邻近方案（重要性加权池化,不改变槽成员划分,只改池化权重）在 niah 上
  定向变差（0.0827→0.0787）——说明问题出在"哪些 token 同槽"，不是"槽内怎么加权"。
  这是本方案（改变槽成员划分）相对该方案（改变槽内权重）的直接论据。

这几个数字是本分支所有实验的**对照锚点**，任何新结果都应该对着这张表汇报：

| 配置 | ACC | LongBench | LongBench_e | niah@32768 |
|---|---:|---:|---:|---:|
| 稠密（CPT 后）| — | — | — | 0.9353 |
| 现有 LogKV vanilla（位置分桶）| 0.6113 | 0.1716 | 0.1918 | 0.0827 |
| SemanticLogKV（本分支目标）| ? | ? | ? | 目标 ≫ 0.0827 |

## 2. 核心设计

### 2.1 语义簇路由（ddCRP：语义 × 时序）+ 簇内 ladder + 非参预算

三层结构，从新到旧：

| 层 | 内容 | 位置表示 | 规模 |
|---|---|---|---|
| recent window | 精确 token，不压缩 | 标准 RoPE | `recent_size`（1024）|
| 簇池 | K 个语义簇，ddCRP 路由 | 每 entry 一组锚点 | `K ≤ K_max` |
| 簇内 ladder | 该簇成员的 Fenwick 层级 | 合并时锚点 min/max 精确维护 | `B′ × L_max` per cluster |

**路由规则（v3：ddCRP，语义相似度 × 时序邻近度）**：token 离开 recent window（flush
时）才参与路由。加入某簇需要**同时**满足两个条件：

```
join(c)  需要   cos(k_raw, centroid_c) ≥ τ     且     n − last_touch_c ≤ Δ
否则 fork：老簇原地冻结，新簇继承 centroid，从当前位置重新积累
```

`last_touch_c` 是该簇最近一次收到成员时的位置。`K` 满且仍需新建时，按 Ward 代价
（`p_a·p_b·‖k_a−k_b‖²`，与现有 `_pair_rank1_stats` 里已经在算的量同构）合并最近的
两个簇腾位。聚类在 **pre-RoPE** key 空间进行——簇身份必须是纯语义的。

**为什么时序门控不是可选项而是前提**：[DeltaKV](https://arxiv.org/abs/2602.08005) 实测
**超过 60% 的语义相似 token 在位置上相距 16 以上**。也就是说纯语义聚类下"成员时间上
散得很开"是常态而非例外。没有时序约束，`(p_lo, p_hi)` 会例行覆盖大半个文档，位置
信息退化成"这个簇全文都有"，等于没有信息。

**为什么是 fork 而不是 refresh（覆盖）**：这是个关键取舍。"前面说考了 100 分，后面
更正说是 90 分"——fork 之后 100 分留在它的旧位置、90 分在新位置，**两者都在，靠位置
锚点区分先后**，模型自己决定信哪个。覆盖等于 cache 替模型做了一个它没授权的、不可逆
的遗忘决定；如果 query 恰好是"更正之前原本报的是多少"，答案已经被毁了。详见 §2.4。

**理论定位**：这正是 [distance-dependent CRP](https://arxiv.org/pdf/0910.1022)
（Blei & Frazier）——标准 CRP 的落座概率只看桌上人数，ddCRP 让它同时依赖距离函数
`f(d_ij)`，把 `d` 取成时间距离就得到本方案。这是 CRP 的推广而非替换，下面的预算
故事完整保留。

**簇内 ladder 是承重墙，不是装饰**：如果只有一层平面簇，needle 一旦没被 τ 隔离、
而是并入一个几千成员的大簇，就被稀释成 1/几千——比现在的 1/32 更糟。簇内 ladder
保证新近成员保持细粒度。与现状的一处语义变化：ladder 的 level 0 存**单 token**
（`w=1`），不是像现有方案那样存"2 token 合并"——这样每个簇的 ladder 是它自己成员
序列上的标准 Fenwick，单成员簇的 entry 永不合并（§3 的核心论据）。

**读出端不感知簇结构**：`log_kv_slot_attention` 只看到一个扁平槽池加一个有效位掩码。
簇只是写路径的路由元数据。这是保持张量矩形、避免 ragged 布局的关键设计（§5.4）。

**预算的理论形状**：DP-means/ddCRP 是 CRP 混合模型的小方差极限，簇数天然满足

```
K_n = 1 + Σ_{i=1}^{n-1} Bern( α / (α+i) )      E[K_n] ≈ α·ln n
```

因为是独立 Bernoulli 之和，Chernoff 直接适用，超出常数倍期望的概率是 n 的多项式
小量——**高概率界**，不只是期望好看。最坏 `K=n`，且只在"每个 token 都远离所有已有
簇"（数据真的没有可压缩结构）时发生，退化是数据挣来的，不是运气差。

**必须提前接受的经验事实（未验证，S0.2 待测）**：自然语言的类型数按 **Heaps 定律**
`~ n^d`（d≈0.4–0.6）增长，不是 log n，对应 Pitman-Yor 而非 CRP。`K_eff` 大概率是
幂律而非对数——`O(√n)` 依然是合格的次线性目标（32k 下约 181 簇，仍远少于现在的约
2560 槽），论文里写 `O(n^d)` 带可调 d 比硬说 log n 更站得住，但这必须实测。**注意
ddCRP 的 fork 机制会进一步推高 K**，S0.2 必须在开启时序门控的条件下测。

**生产实现用硬顶,不是无限期望**：预分配 `K_max × B′ × L_max` 个槽，占用随内容浮动
但上界固定。"不设上限、期望 O(log n)、最坏 O(N)"的版本保留为论文里的曲线和 ablation。

### 2.2 位置表示（v3）：真实锚点集，不做任何平均

**病因先钉死**：v1/v2 的 `z_f = Σ w_j e^{iθ_f p_j} / Σ w_j` 本质上是把多个振荡量叠加
求平均。只要成员位置散布，高频通道上相位就会在圆上转到四面八方而抵消——这是数学
事实，任何"求平均"式的位置统计量都逃不掉。v2 的 `log ρ` 置信项只是让模型**诚实地
知道**相消发生了，并没有阻止它发生。**要结构性杜绝相消，唯一的办法是不做平均。**

**定义**：每个 entry 存该 entry 覆盖范围内**真实成员**的位置锚点（整数，不是统计量）：

```
p_lo   = 该 entry 内最早成员的真实位置
p_hi   = 该 entry 内最晚成员的真实位置
p_mid  = "主要集中在哪"——权重更大一侧继承而来的某个真实成员位置
```

键仍存 **pre-RoPE 内容均值** `k̄_raw`（内容侧的均值没有振荡问题，不需要改）。

**读出**：对同一个 `k̄_raw` 在每个锚点上分别做**标准、未经任何修改的 `apply_rope`**，
生成 M 个虚拟槽：

```
k_eff_a = apply_rope( k̄_raw, cos_cache[p_a], sin_cache[p_a] )     a ∈ {lo, mid, hi}
```

每个虚拟槽用的都是**真实发生过的位置**，`ρ ≡ 1`，相消不可能发生。这比 v2 更彻底也
更简单：不需要复数 buffer、不需要 gain 公式、不需要 `κ`、不需要争论 `pool_f` 怎么
汇总频率——整套机制就是原封不动的 `apply_rope`，只是在几个真实坐标上各调用一次。
**"和 token 级 RoPE 一样优美"在这里是字面意义上的:它就是 token 级 RoPE。**

**合并规则**（比 z 的加权圆均值更干净的代数结构）：

```
merge(A, B):
    p_lo  = min(A.p_lo, B.p_lo)
    p_hi  = max(A.p_hi, B.p_hi)
    p_mid = A.p_mid if A.w >= B.w else B.p_mid      # 永远是某个真实成员的真实位置
```

min/max 是精确、可结合、**幂等**的合并——比 z 的加权复数平均更贴合 Fenwick/segment
tree 这类结构本来就偏好的幂等半格运算：合并顺序和层级都不影响最终值，零累积误差，
且不需要像 z 那样带着权重一起做加权。O(1) 更新，与现有 `_binary_carry` 节奏兼容。

`p_mid` 不是精确中位数，但它永远是真实成员的真实位置，不是合成出来的。是否值得加
第三个锚点是经验问题，由 S0.6 决定（去重后的槽数膨胀倍数）。

### 2.3 打分接入：复用现有 kernel + 一处必须做的 mass bias 修正

每个 entry 展开成 1~3 个虚拟槽（去重后，`p_lo == p_hi` 的年轻 entry 自动只剩 1 个，
不额外花钱），塞进现有的 `log_kv_slot_attention`：

```
score_{s,a} = scale·(q · k_eff_{s,a}) + λ·log( w_s / M_s )   ← 注意分母
value_{s,a} = v̄_s
```

**`/M_s` 是必须做的正确性修正，不是可选优化。** 如果一个 entry 展开成 `M_s` 个虚拟
槽而每个都带着完整的 `w_s`，`+λ·log(w_s)` 会被计入 `M_s` 次，softmax 里这个 entry
实际拿到的质量变成 `M_s · w_s · exp(score)` 而不是设计初衷的 `w_s · exp(score)`
——一个会静默放大大跨度簇权重的 bug。按去重后的锚点数摊薄，让 M 个槽的质量加总
仍对齐 `w_s`。**这个坑是 v3 引入的，v1/v2 不存在，写实现时必须首先处理。**

不再需要 `κ`、不再需要 `log ρ` 置信项——没有任何虚拟槽在"撒谎"，每个都是它所在真实
位置上的诚实读出，置信度问题不存在了，自然也不需要额外机制补偿。v2 那一整条设计链
在 v3 里被消解掉，不是被绕过。

**v3 相对 v2 的诚实代价对比**：

| 维度 | v2（z 统计量）| v3（锚点集）|
|---|---|---|
| 相消问题 | 诚实报告（`log ρ` 置信项）| **结构性不存在** |
| 存储/槽 | `rope_n_elem` 个 fp32（约 128 float）| 2~3 个整数 |
| 额外超参 | `κ`，需要 CPT 校准 | 无 |
| 命中"典型/中心"位置 | 好（圆均值是 MMSE 点估计）| 弱（只有离散锚点，中间空隙无代表）|
| 命中"边界"位置 | 弱（平均会模糊边界）| **精确**（边界就是真实成员）|
| 槽数开销 | ×1 | 去重后 ×1~3（**S0.6 必测**）|
| mass bias | 不需额外处理 | 必须按 `M_s` 摊薄 |

核心的偏差-方差权衡：z 是"平滑但可能虚构"的中心估计，v3 是"精确但离散"的边界估计。
一个成员大致均匀散布在整个跨度里的簇，查询若想对齐"跨度中间"的内容，z 平均出的相位
可能刚好命中而 v3 的边界锚点谁都对不上——这是真实存在、需要接受的短板，不是可以
设计掉的。z 因此保留为 §7 的正式消融对照，不是被删除。

### 2.4 开放问题：均值池化无法表达 supersession（未定，S0.7 判定）

**问题是真的**：均值池化无法表达"后面的信息作废前面的"。"考了 100 分"和"更正为
90 分"平均成 95，是一个从未存在过的数。[Gated DeltaNet](https://arxiv.org/pdf/2412.06464)
的更新规则

```
S_t = α_t (I − β_t k_t k_tᵀ) S_{t−1} + β_t k_t v_tᵀ
```

里的 Householder 项 `(I − βkkᵀ)` 干的正是"先擦掉当前 key 方向上已有的关联、再写入
新值"。现有设计确实没有回应这个缺陷。

**但"每簇维护一个 gated delta state"这个方案暂不采纳**，三条理由（第 2 条是决定性的）：

1. **KV cache 不该替模型做遗忘决定。** DeltaNet 擦除是因为它的 state 是**固定大小**
   的，容量硬约束下覆盖旧关联才最优。SemanticLogKV 的前提是"次线性但会增长"的内存，
   它**付得起**把 100 和 90 都以更粗的分辨率留着。擦除是在不知道未来 query 的情况下
   做的不可逆承诺。
2. **读出机制不兼容，且不兼容的方向恰好是捞针。** 线性 state 的读出 `qᵀS` 是所有
   写入的叠加，没有选择性——这正是线性注意力/Mamba 召回差的根因，也正是 Log-Linear
   Attention 当初要用 Fenwick 树而非单一 state 的原因。更尖锐的是：**簇内 key 高度
   相似恰恰是线性 state 容量的最坏情况**（近平行 key 相互干涉最严重）。具体后果：
   needle 若因 τ 太松落进 haystack 大簇，均值池化下它至少贡献 1/w，delta rule 下会被
   后续写入沿几乎相同的 key 方向**彻底擦除**——τ 失手的代价从"稀释"（原则上可恢复）
   恶化成"抹除"（不可恢复），在最想保护的指标上反向加杠杆。
3. **训练路径代价。** 现有 `LogKVStreamTrainingAttention` 是 detached state + 自定义
   backward。delta state 要让梯度穿过 recurrence（chunkwise parallel delta rule），
   是训练路径的重写而非增量改动，也是最容易引入训推不一致的地方（pin 一号死因）。

**能保留这个洞察的落地方式（推荐路径）**：现有的 rank-1 Γ 修正**结构上已经是一个
秩 1 的线性 state** 了——

```
read_s = v̄_s + scale·γ_s·(q · γa_s)·γb_s
                          └── 就是 qᵀ(γa γbᵀ)，一个秩 1 外积的线性读出
```

值的读出已经依赖 query 方向。所以诉求不需要新结构，只需改 `Γ` 的**构造方式**：现在
它从二阶矩/协方差算出，改成用 delta-rule 式更新（让后进成员在冲突方向上对先进成员
有擦除权）。这样均值 `v̄` 作为不可擦除的底座保留、softmax 选择性读出保留、改动局限
在 `_pair_rank1_stats`/`compact` 内部，不新增张量、不动训练路径。

**一个无条件正确、零副作用的采纳点**：`_update_centroid` 现在用在线均值，而一个话题
已漂移的老簇，其 centroid 本就该被新成员**替换**而非平均。centroid 是路由元数据、
不参与 attention 读出，擦除它不丢任何信息——这里直接上门控更新，没有争议。

**判定实验 S0.7**：在真实 prompt 形成的簇里统计成员 value 是**互相并存**（均值合理）
还是**系统性作废**（delta rule 有价值）。前者罕见则 delta rule 在解决不存在的问题；
后者普遍则 Γ 广义化的优先级要提。**这个分歧靠实验解决，不靠辩论。**

## 3. 为什么能解决捞针

逐项对比三种情形下 needle 的命运。

**稠密注意力**：`score = scale·(q·k_needle)`，`value = v_needle`。needle 靠
`q·k_needle` 显著高于 haystack 而胜出，读出的是它自己的 value。

**现有池化（needle 落在宽度 w 的槽里）**：

```
k_slot   = ( k_needle + Σ_{j≠n} k_j ) / w
q·k_slot = (q·k_needle)/w + (w-1)/w · (q·k̄_hay)
v_slot   = ( v_needle + Σ_{j≠n} v_j ) / w
```

分数侧和读出侧**同时**被稀释 w 倍。`+λ log w` 补的是计数质量而非内容,补不回稀释；
即使这个槽赢得注意力,返回的 `v_slot` 也已经是 haystack 均值,而 Γ 修正是 rank-1,
救不回一个被 w 倍稀释的方向。这解释了为什么二阶修正把 niah 从 0.032 提到 0.0827
（2.6×）却依然离 0.9353 极远：它修的是分数,修不了读出。

**SemanticLogKV（v3）**：needle 与所有 haystack centroid 余弦 `< τ` ⇒ 自成一簇 ⇒
成员数=1 ⇒ ladder 永不填满 ⇒ entry 永不合并；且 `p_lo = p_hi = p_mid` ⇒ 去重后
`M = 1`：

```
score = scale·(q · k_needle) + λ·log(1/1) = scale·(q · k_needle)   ← 与稠密逐位相同
value = v_needle                                                    ← 与稠密逐位相同
位置  = 在 p_needle 上的标准 RoPE                                    ← 与稠密逐位相同
```

**分数、读出、位置三者同时恢复到稠密**,这是"needle 自成簇"这个结构带来的,不是靠
调权重或加修正项凑出来的。v3 相对 v2 的额外好处：这个论证不再依赖任何超参（v1 要
指定 β、v2 要指定 κ，v3 什么都不用指定）。

代价只有一个槽——haystack 语义高度同质、`K_eff` 小、压得很狠,省下的预算转移给异常
点。**这就是为什么自适应预算不是锦上添花而是唯一机制**：固定均匀预算在信息论上无法
同时做到"压缩 haystack"和"保留 needle",因为它不允许预算转移。

**失败模式（必须在 Stage 0 证伪）**：τ 太松 → needle 并入 haystack 簇 → 退回稀释。
τ 是全方案最敏感的超参,S0.3 就是为它设计的。

## 4. 最坏情况的双重兜底

**表现的地板**：内容不可压缩时 `K` 增长 → 槽变多 → 质量向稠密平滑逼近。`K` 撞上
`K_max` 时强制 Ward 合并,把结构推回"少数大簇 + 位置序 ladder",等价 `K_max=1`
锚点,即**今天的 LogKV 行为**。退化的终点是现状,不是比现状更差。

**性能的天花板**：硬性 `S_max`，占用随内容浮动但上界固定。**v3 新增的风险是锚点
展开会让实际槽数翻 1~3 倍**：

| 配置 | 槽数 | 说明 |
|---|---:|---|
| 现有 LogKV recent + B=512 × ~5 级 | ≈ 3584 | 对照基线 |
| SemanticLogKV recent 1024 + K16×B′8×L13（entry 数）| ≈ 2688 | 预分配上界 |
| 同上，锚点展开最坏 ×3 | ≈ 6016 | **超过基线，不可接受** |

三层缓解手段，但**都需要 S0.6 的数据支撑而不是拍脑袋**：
1. 去重天然省钱——年轻/窄跨度 entry 自动收敛成 1 个锚点。但 Fenwick ladder 每层
   槽数大致均衡，老的宽跨度 entry 占比不一定小，真实平均倍数必须实测。
2. 多锚点只用在**簇级**（粗粒度），簇内 ladder 保持单锚点。
3. 相应调低 `K_max`/`B′`/`L_max` 把总槽数摁回对齐基线。

> **论文层面最大的攻击面**：自适应预算意味着 niah 变好可能仅仅因为多用了内存——
> 而 niah 恰恰最容易把 K 推高。**从第一个实验开始就必须报告每个 benchmark 上的
> 实际占用槽数（含锚点展开后的真实数字）**,并准备 memory-matched 对照。这一条
> 如果留到最后补,整套实验要重跑。

## 5. 实现方案

### 5.1 新模块 `litgpt/log_kv_position.py`（v3 版，比 v2 大幅简化）

无状态、无可学参数、CPU 可测。v3 不再需要复数统计量、gain 公式、置信度函数。

```python
# 锚点合并：精确、可结合、幂等。比 v2 的加权复数平均更干净
def merge_anchors(lo1, hi1, mid1, w1, lo2, hi2, mid2, w2):
    lo  = torch.minimum(lo1, lo2)
    hi  = torch.maximum(hi1, hi2)
    mid = torch.where(w1 >= w2, mid1, mid2)   # 永远是某个真实成员的真实位置
    return lo, hi, mid


# 锚点 -> 虚拟槽键：就是标准 RoPE，在真实位置上取现成的 cos/sin cache 行
def materialize_anchor_keys(k_raw, anchors, cos_cache, sin_cache, rope_n_elem):
    """anchors: (..., S, M) int64 真实位置; 返回 (..., S*M, d)
    每个锚点一次 apply_rope —— rho 恒为 1，不存在相消。"""
    cos = cos_cache[anchors]        # (..., S, M, rope_n_elem)
    sin = sin_cache[anchors]
    x   = k_raw.unsqueeze(-2)[..., :rope_n_elem]
    h   = rope_n_elem // 2
    rot = torch.cat((-x[..., h:], x[..., :h]), -1)
    roped = x * cos + rot * sin
    return torch.cat([roped, k_raw.unsqueeze(-2)[..., rope_n_elem:].expand_as(...)], -1)


# 去重 + mass bias 摊薄因子：M_s 是该 entry 去重后的有效锚点数
def dedup_anchors(lo, hi, mid):
    """返回 (unique_anchors, M) —— p_lo==p_hi 的年轻 entry 自动收敛为 M=1。
    调用方必须用 log(w / M) 而不是 log(w) 做 mass bias（§2.3）。"""
    ...
```

**单测（Stage 0.1，纯 CPU，不依赖 dump）**：
- **嵌套精确性**：单 token entry（`p_lo=p_hi=p_mid`）→ 去重后 M=1，输出与
  `model.apply_rope` 在该位置上逐位一致。
- **合并的幂等性与结合律**：任意二叉合并顺序结果一致；`merge(A,A) == A`。
- **锚点永远真实**：随机构造合并树，断言每个输出锚点都属于原始成员位置集合。
- **mass bias 守恒**：M 个虚拟槽在 `λ·log(w/M)` 下的 softmax 质量加总，等于单槽
  在 `λ·log(w)` 下的质量（这是 §2.3 那个 bug 的回归测试，**必须有**）。
- **v2 对照**：保留 z 统计量实现与其 Dirichlet 闭式解单测，供 §7 消融使用。

### 5.2 `litgpt/log_kv_cache.py`

- 新增 buffer：每 entry `(p_lo, p_hi, p_mid)` int32、每 entry `cluster_id`、每簇
  `centroid`（`(B,G,K_max,d)` L2 归一）、`cluster_size`、**`last_touch`**（v3 新增，
  ddCRP 时序门控用）。
- 键改存 **pre-RoPE**。recent window 同样存 pre-RoPE + 位置，读出时统一走
  `materialize_anchor_keys`——**一条代码路径**，保证嵌套性质在生产里被真实执行。
- `compact()`/`_binary_carry()` 增加锚点合并（一行 `merge_anchors`），Σ/Γ 全部改在
  **pre-RoPE 内容空间**统计，物化时对 `σu`、`γa` 施加与 `k̄` 相同的旋转（rank-1 在
  线性映射下仍是 rank-1）。**顺带的好处**：Σ 不再混入位置相位方差，簇内同质时
  rank-1 近似精度应系统性恢复——这是另一分支 D1 自检里 width≥8 失真的根因之一。
- 路由（`@torch.no_grad()`，与 cache 更新同路径，训推自动一致——这是从 pin 系列
  教训里吸取的设计原则，见 §9）：

```python
@torch.no_grad()
def _route_ddcrp(self, k_raw, pos):
    """k_raw: (B, G, m, d) 刚离开窗口的 token。m 通常为 2，顺序处理即可。
    聚类在 pre-RoPE 空间进行 —— 簇身份是纯语义的，这只有解耦之后才可能。"""
    for t in range(k_raw.size(2)):
        kt  = F.normalize(k_raw[:, :, t], dim=-1)
        sim = torch.einsum("bgd,bgkd->bgk", kt, self.centroid)
        stale = (pos[t] - self.last_touch) > self.delta        # ddCRP 时序门控
        sim = sim.masked_fill(stale, -1.0)                     # 打断过的簇不许 join
        best, idx = sim.max(-1)
        need_new = (best < self.tau) & (self.cluster_size.gt(0).sum(-1) < self.K_max)
        idx = torch.where(need_new, self._first_free_slot(), idx)
        self._append_to_cluster(idx, k_raw[:, :, t], pos[t])
        self._update_centroid(idx, kt)          # §2.4：老簇宜用门控替换而非在线均值
        self.last_touch[..., idx] = pos[t]
        # K 满且 best < tau：先 Ward 合并最近两簇腾位，再新建
```

- `get_attention_state()` 额外返回 `(B,G,S)` 有效位掩码与每 entry 的 `M_s`；
  `log_kv_slot_attention` 增加可选的槽有效性掩码参数（fp32 分数上填 `-inf`），
  与现有 `causal_tail` 正交；**mass bias 改用 `λ·log(w_s / M_s)`（§2.3）**。

### 5.3 `litgpt/model.py`

唯一的接口性改动：cache 现在需要 **pre-RoPE 的 k** 和该 token 的**绝对位置索引**
（v2 需要的是 cos/sin 切片，v3 只需要整数位置，更简单）。模型本来就持有这两样，
所以是传参改动而非新计算。`build_log_kv_cache`/`set_log_kv_cache`/
`enable_log_kv_training` 三处透传新参数。读出侧需要访问 RoPE cache 做锚点物化。

### 5.4 矩形张量与 batching

- **按 (B, G) 独立聚类**——不同头关注不同语义，共享簇是错的。
- **预分配 + 掩码**：槽张量固定为 `K_max × L_max × B′ × M_max`，占用不满时掩码
  屏蔽。张量始终矩形，不引入 ragged 布局。
- ragged/paged 布局留到工程化阶段，不在研究验证期做。

### 5.5 参数与开关

沿用项目惯例：`demo.py`/`eval.py` 走 `run_cli()` 签名自省与 `_o()`，**YAML 新字段
默认必须写 `null`**（否则会被同名 CLI 参数静默覆盖，见 §9），不进 `eval.sh`/
`majob.sh` 核心列表，走 `DIAG_ARGS` opt-in。

| 参数 | 默认 | 作用 |
|---|---|---|
| `log_kv_semantic_clusters` | `false` | 总开关，关闭时逐字节复现改动前行为 |
| `log_kv_cluster_k_max` | `16` | 簇数硬顶 = 性能天花板 |
| `log_kv_cluster_tau` | `0.6` | 语义阈值，最敏感的超参 |
| `log_kv_cluster_delta` | `2048` | **v3 新增**：ddCRP 时序门控窗口 |
| `log_kv_anchor_mode` | `lo_hi_mid` | **v3 新增**：`lo_hi_mid` / `lo_hi` / `mid` / `z`（消融）|
| `log_kv_cluster_entries` | `8` | 簇内每级 entry 数 B′ |

## 6. 实验协议

四个阶段，每阶段带可证伪的决策门。顺序不能打乱：Stage 0 几乎不花 GPU，却能在写任何
生产代码之前否掉整个方案。

### Stage 0 — 离线可证伪（1 次 GPU dump + 全部 CPU 分析）

只需要一次前向：对几条 32k 的 NIAH prompt dump 每层每头的 **pre-RoPE k / v** 与
needle 的 token span（复用另一分支已有的 `log_kv_pin_diag.py` 定位逻辑）。之后所有
分析在 CPU 上做。

| 编号 | 测什么 | 为什么 |
|---|---|---|
| S0.1 | §5.1 单测（含 mass bias 守恒回归测试）| 数学正确性，纯 CPU，不需要 dump |
| S0.2 | `K_eff(n)` 随 τ、Δ 的曲线，拟合 log/幂律/线性 | 预算故事成不成立（**必须开着 ddCRP 测**）|
| S0.3 | needle 隔离率：所在簇的 `w` 分布 | §3 的核心机制成不成立 |
| S0.4 | 簇内内容方差 / 现有位置槽内方差 | 语义分组相对位置分组的增益上界 |
| S0.5 | 簇的 `(p_hi − p_lo)` 跨度分布，开/关 ddCRP 对比 | 验证时序门控确实压住了跨度 |
| **S0.6** | **锚点去重后的平均倍数 `E[M]` 与分布** | **v3 特有：内存会不会失控（§4）** |
| **S0.7** | **簇内 value 的并存 vs 作废比例** | **§2.4 开放问题的判定实验** |

**决策门**：
- **S0.2**：32k 下 τ∈[0.5,0.8] 时 `K_eff` 应在 10² 量级。若达 10³ 以上，预算故事
  垮掉——回去把聚类粒度调粗再谈。
- **S0.3**：needle 落在 `w≤4` 的簇里的比例应显著高于随机基线。若 needle 大多并入
  大簇，§3 的机制不成立，方案应就地停止，不要硬着头皮训。
- **S0.5**：开 ddCRP 后跨度中位数应显著小于关闭时。若无差别，说明时序门控没起作用，
  Δ 需要重调；若跨度依然接近全文，锚点表示的信息量存疑。
- **S0.6**：`E[M]` 应 < 1.6。若接近 3，必须走 §4 的缓解手段（多锚点只用于簇级），
  否则内存对不齐基线，整套实验的公平性没了。
- **S0.7**：若簇内 value 系统性作废的比例 > 30%，把 Γ 的 delta-rule 广义化提到
  Stage 1 范围内；否则记录结论并搁置 §2.4。

### Stage 1 — 实现

按 §5 落地，默认关闭时逐字节复现现状。额外必测：`K_max=1` 且路由永远选中同一簇
（退化成单一位置序 ladder）时，数值上应逼近现有位置分桶实现。

### Stage 2 — eval-time 探测

诚实的预期：新表示对现有 CPT 权重是分布外的，绝对分数可能不好看。价值在于**形状而非
绝对值**，Config A 是硬性正确性闸门。

| 配置 | 参数 | 期望 |
|---|---|---|
| A 正确性闸门 | `K=1`（强制单簇）| ≈ 0.1716 / 0.0827。**对不上就是实现有 bug，不要往下走** |
| B 无时序门控 | `K=16, Δ=∞` | 隔离 ddCRP 的贡献 |
| C 主实验 | `K=16`，扫 `τ` 和 `Δ` | niah 应显著高于 A |

```bash
DIAG_ARGS="--log_kv_semantic_clusters true --log_kv_cluster_k_max 16 \
  --log_kv_cluster_tau 0.6 --log_kv_cluster_delta 2048" \
    bash majob.sh exp/qwen1.7b-32k/arc_warmup.yaml
```

沿用另一分支 §8.5 记录的坑：主 benchmark 参数不要传 `none`（LongBench 来自主列表）；
多配置串行跑（输出目录不按超参分开，靠 JSON 内的 metadata 区分）；新参数需要一并
写进 eval 的 metadata 字段。

### Stage 3 — CPT

Stage 2 有信号后再投入。v3 没有需要 warmup 的新标量（v2 的 `κ` 已被消解），所以
训练侧风险低于 v2；若 S0.7 触发了 Γ 广义化，那部分沿用 `second_order_scale` 的
爬坡机制（目标值别设太激进、warmup 别只给 10 步——另一分支 §4 真实踩过的崩溃教训）。

## 7. 消融表

每一行回答一个独立问题，不是穷举网格。所有行都必须附带**锚点展开后的实际槽数**（§4）。

| 旋钮 | 取值 | 回答的问题 |
|---|---|---|
| `K_max` | 1 / 4 / 16 / 64 | 语义分组本身值多少分？1 是现状锚点 |
| `anchor_mode` | `lo_hi_mid` / `lo_hi` / `mid` / `z` | **锚点表示 vs v2 的 z 统计量，验证 v3 确实优于 v2** |
| `Δ`（ddCRP 窗口）| 512 / 2048 / 8192 / ∞ | 时序门控值多少分；∞ 是纯语义 DP-means 对照 |
| `τ` | 0.4 – 0.85 | needle 隔离与簇纯度的平衡点 |
| `λ`（mass bias）| 0 / 1 | 大簇的计数质量补偿是否仍然正确 |
| rank-1 Σ/Γ | 关 / 现有构造 / delta-rule 构造 | 解耦后二阶修正的边际价值；第三档取决于 S0.7 |
| vanilla memory-matched | B 调大到同槽数 | **排除"只是多用了内存"** |

指标沿用现有四项（ACC/LongBench/LongBench_e/niah@32768），**另加 multi-needle**——
单 needle 一旦从 0.08 提上去就会迅速失去区分度，multi-needle 才是这类分层压缩方法
真正拉开差距、也最被审稿人看重的实验。v3 的锚点表示对 multi-needle 应有额外优势
（多个 needle 各自成簇、各自保留精确位置），这是值得单独讲的故事。

## 8. 风险与对策

| 风险 | 后果 | 对策 |
|---|---|---|
| **τ 敏感** | needle 并入大簇，机制失效 | S0.3 提前证伪；τ 进主扫描 |
| **锚点展开撑爆内存**（v3 新增，最大工程风险）| 槽数 ×3，内存对不齐基线，实验公平性没了 | S0.6 先测；缓解见 §4 三层手段 |
| **mass bias 未按 M 摊薄**（v3 新增，静默 bug）| 大跨度簇权重被放大 M 倍 | §5.1 的守恒单测是硬性 gate |
| **锚点之间的"空隙"无代表** | 成员均匀散布的簇，查询对齐中段时无锚点可命中 | 已知短板（§2.3），`p_mid` 部分缓解；z 作为消融对照量化差距 |
| **ddCRP 推高 K** | 预算故事被自己的机制打穿 | S0.2 必须开着时序门控测；Δ 进主扫描 |
| **centroid drift** | 早期成员归属基于后来会漂移的 centroid | 在线聚类固有近似；§2.4 的门控 centroid 更新是零副作用的改进 |
| **内存不对齐比较** | 论文被质疑"只是多用了内存" | 见 §4，从第一个实验起报告真实槽数 + memory-matched 对照 |
| **训练期显存随内容波动** | batch 内簇占用不同 | `K_max` 预分配上界固定，训练期必须开硬顶 |

## 9. 复用另一分支的基础设施与教训（精简版）

只保留对本分支**操作上仍然生效**的结论（完整版在 `claude/semantic-cluster-log-
compression-8ai32a` 分支的 CLAUDE.md）。

**RoPE 布局约定**：`apply_rope`（`litgpt/model.py:2074`）是"前后半重复"布局，
`cos[f]==cos[f+d/2]`，partial rotary 只旋转前 `rope_n_elem` 维、尾部内容通道不旋转。
§5.1 的锚点物化直接依赖这个约定。

**pin 系列四条死因（设计规避清单，本方案逐条对照）**：
1. 训推不一致——pin 选择只在推理期跑，训练从未见过。本方案：路由同样 `no_grad`，
   但训练和推理走**同一条** cache 更新路径，从设计上不存在"训练没见过"。
2. 分数尺度失配——pin 槽是精确 key，混进池化槽的 softmax 行时点积尺度不同。
   本方案：槽种群完全同质（都是池化槽，包括单点簇）。
3. 观察窗口的架构性局限——pin 依赖一次性 dense prefill 和"prompt 尾部是问题"两个
   假设。本方案：路由在 flush 时逐 token 发生，不需要看到整段 prompt。
4. 剂量效应——pin 插得越多伤害越大。本方案：不新增槽种类，只改变成员划分。
   **注意 v3 的锚点展开在形式上有点像"插入更多槽"，但它们共享同一个 `v̄` 和被摊薄的
   mass bias，不是独立的新信息源，与 pin 的剂量效应不同源。**

**工程习惯**：
- `eval.sh` 把 YAML 展平成纯 CLI flag（不传 `--config`），`_o()` 的"YAML 非 null
  覆盖 CLI"逻辑不会触发；直接用 `--config <yaml>` 会触发。新增会被扫参覆盖的字段，
  YAML 里必须写 `null`。
- `majob.sh` 如果 `save_path` 下已有 checkpoint 会跳过训练直接 eval——新实验必须用
  独立 `save_path`，不要复用另一分支的目录。
- 本地跑 pytest 用 conda env `mineru`（`/Users/hourunli/anaconda3/envs/mineru`，
  Python 3.12）；系统自带 Python 无法解析仓库里到处用的 `X | None` 类型注解。

## 10. 相关工作定位（2026-08 快照，务必用 search 复核最新情况）

| 工作 | 做了什么 | 与本方案的区别 |
|---|---|---|
| SemantiCache (2603.14303) | 按分隔符切语义块 + 贪心种子聚类合并 | 固定 budget，无簇内分辨率层级，frozen model |
| Multipole Attention (NeurIPS 2025, 2506.13059) | key 做 k-means，远处用 centroid 近似打分 | **保留全量 KV（O(n) 显存）**，centroid 只是索引，不是压缩 |
| SeKV (2606.31145) | entropy 引导语义 span，GPU summary + CPU SVD 重建 | offload + 检索系统，什么都不丢，非次线性 |
| ClusterAttn (ACL 2025) | 密度聚类自适应簇数 | 稀疏注意力，固定 1024 token 预算 |
| GVote (ICLR 2026, 2509.03136) | 蒙特卡洛采样未来 query 免手工设预算 | eviction/selection 框架，预算仍是 O(1) 固定池 |
| **DeltaKV (2602.08005)** | 残差编码 KV，利用长程 token 相似性 | 不是次线性；**但它实测的"60% 相似 token 相距 >16 位置"是本方案 §2.1 时序门控的实证依据** |
| **Gated DeltaNet (2412.06464) / GDN-2 (2605.22791)** | 门控 + delta rule 的固定大小线性 state | 固定容量下的覆盖式记忆；§2.4 详述为何不直接采用，以及可借鉴的部分 |
| **ddCRP (0910.1022)** | 距离依赖的 CRP，落座概率含距离函数 | 本方案 §2.1 路由规则的理论出处 |

空着的生态位：**非参先验决定簇数 + 严格次线性空间 + 簇内分辨率层级 + 不做平均的
真实锚点位置表示**。CRP/ddCRP 的集中不等式给了这条线一个别人写不出的理论节。多次
搜索没有找到任何工作用非参贝叶斯给簇数一个先验、同时保持严格次线性显存。

## 11. 变更记录

> 每次讨论产生突破或进展,在这里加一条,新的在最上面。只记"改变了什么结论/设计",
> 不重复已经写进正文的细节——细节改到对应章节,这里留指针和一句话动机。

- **2026-08-14｜v2 → v3：位置表示从"求平均的 z 统计量"改为"不做平均的真实锚点集"，
  路由从纯语义 DP-means 改为 ddCRP 时序门控。** 动机：用户指出 v2 的 z 仍然是加权
  求和，相位相消依然存在，只是被诚实报告而已，要求一种"能表现距离、且结构上不存在
  簇内相消"的表示。解法：放弃合成任何"平均位置"，只存真实成员的 `(p_lo, p_hi, p_mid)`，
  读出时在每个真实位置上做标准 `apply_rope`（ρ≡1，相消不可能发生）；合并用 min/max，
  是幂等半格运算，比加权圆均值更贴合 Fenwick 结构。用户同时提出"语义相近但中间被
  别的簇打断就该新开簇"，这对应 ddCRP，且被 DeltaKV 实测的"60% 相似 token 相距 >16
  位置"证明是**前提而非补丁**（否则锚点跨度会覆盖全文而失去信息量）。
  连带产出：①发现 mass bias 必须按去重锚点数 `M_s` 摊薄，否则大跨度簇权重被静默
  放大 M 倍（§2.3，v3 引入的新 bug，已列为硬性单测）；②v2 的 `κ`/`log ρ` 置信项
  整条被消解，不再需要（v3 没有槽在"撒谎"）；③新增 S0.6（锚点膨胀倍数）与 S0.7
  （簇内 supersession 频率）两项 Stage 0 测量。z 保留为 §7 正式消融对照。
- **2026-08-14｜新增 §2.4 开放问题：均值池化无法表达 supersession。** 动机：用户提出
  "考 100 分→更正为 90 分"这类后信息作废前信息的场景，均值池化会得到从未存在过的
  95，并提议每簇维护 gated DeltaNet 线性 state。结论：**问题真实，但该方案暂不采纳**
  ——线性 state 的 `qᵀS` 读出没有选择性，恰是捞针最弱的机制，且簇内 key 近平行是
  线性容量最坏情况，会让 τ 失手的代价从"稀释"恶化成"不可恢复的抹除"；另外 KV cache
  不该替模型做不可逆的遗忘决定。可行的替代路径：现有 rank-1 Γ **结构上已是秩 1 线性
  state**，改它的构造方式即可获得擦除能力而不动读出机制与训练路径。另有一个零副作用
  的采纳点：centroid 更新宜用门控替换而非在线均值。分歧交由 S0.7 判定，不靠辩论。
- **2026-08-14｜v1 → v2：位置重物化从 β 插值旋钮改为满模长旋转 + log ρ 独立置信项。**
  （已被 v3 取代，保留作为迭代记录）动机：v1 的 `gain=ρ^{(β-1)}` 在 β=0 时对最不可信
  的频率通道施加最大重物化力度，是方向性错误。修法：利用"圆均值相位是原则性点估计、
  与 ρ 无关"这一事实，把旋转固定为满模长，把 ρ 的不确定性改走独立加法项进分数。
- **2026-08-14｜方案定型：语义簇 + 簇级抽象位置,取代最初讨论的"连续性约束 Ward
  合并"方案。** 动机：用户明确偏好语义簇路线的"优美性"。Ward 合并降级为簇内 ladder
  的可选合并顺序策略（正交、可叠加，非互斥）。
- **2026-08-14｜建档。** 从 `claude/semantic-cluster-log-compression-8ai32a` 分支切出
  `semanticLogKV`，本文件取代该分支的 CLAUDE.md 作为独立存档。
