# SemanticLogKV 开发指导文档（存档，随讨论推进持续更新）

> 本文件是 `semanticLogKV` 分支的唯一存档文档，记录"语义簇 + 簇级抽象位置"这条压缩
> 方案的设计、算法规格、实现进度、实验结果和踩过的坑。**每次讨论出现突破或进展，
> 都要写回本文件**（新增到 §13 变更记录，并同步更新受影响的正文章节），不要另开文档。
>
> 本文件只保留 SemanticLogKV 直接相关的内容。原 `logKV`/`claude/semantic-cluster-
> log-compression-8ai32a` 分支上的 CLAUDE.md 是另一条独立技术路线（位置分桶 +
> rank-1 二阶修正 + pin 机制）的进展存档，与本分支无关，不在这里维护——那条线的
> 结论只在本文档 §1、§11 里作为背景锚点和教训引用，不展开叙述。

## 0. 现状速览（2026-08-14）

**这是什么**：LogKV 现有压缩机制（`litgpt/log_kv_cache.py`）按**位置**做 Fenwick 树式
分层压缩——固定 2:1 合并，越老的 token 槽越宽、压得越狠。已验证的结论（另一分支的
存档）：这套方案的最大瓶颈是**槽内异质性**——语义无关的 token 被硬塞进同一个位置槽
做均值池化，needle 稀释成 1/width，niah@32768 从稠密上限 0.9353 掉到 0.0827。

**本分支的方案**：把"按位置分桶"换成"按语义分簇"——语义相近的 token 才共享槽，
needle 因为和周围内容语义不相关而倾向于自成一簇，槽内不再异质，稀释问题从结构上
消失。配套需要解决两个新问题：①位置信息不能再靠 post-RoPE 均值池化表示；②簇数
不能固定，需要由内容驱动、预算仍是次线性的自适应机制。

### 设计迭代史（四轮，v3.1 是当前版本）

| 版本 | 位置表示 / 路由 | 为什么被取代 |
|---|---|---|
| v1 | 逐频率复数统计量 `z`（特征函数）+ 标量 `β` 插值 gain | β 把"往哪转"和"多可信"耦合进一个标量，且 β=0 时对最不可信的频率通道施加最大重物化力度，方向性错误 |
| v2 | `z` + 恒定满模长旋转 + 独立的 `κ·log ρ` 置信项 | 修好了"不确定性污染内容"，但 **z 本身仍是加权和，相位相消依然发生**，只是被诚实报告出来而已 |
| v3 | 真实锚点位置集 `(p_lo, p_hi, p_mid)`；路由加时序门控，打断即 **fork 新簇** | 锚点表示保留；但 fork 把**簇身份**和**存储局部性**绑死，高频复现实体会炸掉 K（§2.1 反例一）|
| **v3.1（当前）** | 锚点集不变；路由拆成 **cluster（语义身份）/ segment（时序段）两层** | — |

**v3 的核心转变（保留）：不再试图"把多个位置压成一个数"，而是不做任何平均。** 每个
entry 存它覆盖范围内**真实成员**的边界与集中锚点，读出时在这些真实位置上各做一次
标准 `apply_rope`。每个虚拟槽的 `ρ` 恒为 1，**相位相消从数学上根本不会发生**——不是
被压制，是压根没有"求平均"这一步给它可乘之机。完整推导见 §2.2/§2.3。

**v3.1 的核心转变**：时序打断不再 fork 出新的**簇**，而是在同一个簇下开一个新的
**segment**。簇只负责语义身份（一个 centroid，用于路由），segment 负责存储局部性。

**当前阶段：设计与算法规格已完成（§5），代码尚未开始写。**

**下一步（按优先级）**：
1. **S0.0（§7）：扫 `η`（时序权重）。** 全课题最根本的实验——`η→0` 是纯语义聚类，
   `η→∞` 退化成"连续性约束语义分段"，扫它等于直接回答"收益来自语义分组本身，还是
   仅仅来自更好的分段边界"。纯 CPU 可测，**排在所有事情之前**。
2. Stage 0 其余离线证伪实验（§7）——一次 dump + CPU 分析，决定方案值不值得往下做。
3. `litgpt/log_kv_position.py` 纯函数 + 单测（§5.4），CPU 可测，不依赖 dump 结果。
4. 视 Stage 0 结果决定是否继续 Stage 1（生产代码）。**动手前先读 §12**——其中 A、B
   两条必须在写第一行代码之前就把方案定死，事后补会非常痛。

## 1. 背景：为什么从位置分桶转向语义分簇

（完整证据链在另一分支的 CLAUDE.md 里，这里只摘录作为本方案设计依据的结论性数字。）

- 稠密 attention 上限（CPT 后模型）：niah@32768 = **0.9353**。
- 现有 LogKV（位置分桶 + rank-1 二阶修正，无 pin）：niah = **0.0827**，LongBench =
  **0.1716**，LongBench_e = **0.1918**，ACC = **0.6113**。压缩本身吃掉 85+ 个百分点，
  是全链路最大的单一瓶颈，比 pin 全系列实验的影响大一个数量级。
- 机制诊断（D1 自检）：width≥8 时 rank-1 近似失真明显；根因是均值池化把 needle
  稀释成 1/width，二阶修正是 rank-1、只能加不能减，救不回一个已经被稀释到"不参与
  softmax 竞争"的槽,尤其救不回**读出侧**（`v_slot` 同样被稀释,Γ 修正同样是 rank-1）。
- 已经试过的邻近方案（重要性加权池化,不改变槽成员划分,只改池化权重）在 niah 上
  定向变差（0.0827→0.0787）——说明问题出在"哪些 token 同槽"，不是"槽内怎么加权"。

这几个数字是本分支所有实验的**对照锚点**：

| 配置 | ACC | LongBench | LongBench_e | niah@32768 |
|---|---:|---:|---:|---:|
| 稠密（CPT 后）| — | — | — | 0.9353 |
| 现有 LogKV vanilla（位置分桶）| 0.6113 | 0.1716 | 0.1918 | 0.0827 |
| SemanticLogKV（本分支目标）| ? | ? | ? | 目标 ≫ 0.0827 |

## 2. 核心设计

### 2.1 路由：cluster（语义身份）/ segment（时序段）两层

四层结构，从新到旧：

| 层 | 是什么 | 位置表示 | 规模 |
|---|---|---|---|
| recent window | 精确 token，不压缩 | 标准 RoPE | `recent_size`（1024）|
| cluster | 一个 centroid，纯语义身份，**只用于路由** | 无（不直接参与读出）| `K ≤ K_max` |
| segment | 该 cluster 一段不被打断的连续访问 | 继承自它的 entry | 每 cluster 若干 |
| entry | ladder 条目（**每 cluster 一条 ladder，不是每 segment 一条**）| `(p_lo, p_hi, p_mid)` | `B′ × L_max` per cluster |

#### 为什么必须拆成两层（v3 → v3.1 的动机，反例一）

v3 把"时序被打断"直接实现成"fork 一个新簇"，会在**高频复现实体**上炸掉预算：一份
文档里"Alice"被提到 50 次、每次都被别的内容打断，就产生 **50 个语义完全相同的 Alice
簇**。三个后果：

1. `K` 正比于话题切换次数而不是 `log n`，预算故事被自己的机制打穿；
2. **内容越重要越会复现，于是越贵**——激励方向是反的；
3. 撞上 `K_max` 后 Ward 合并的必然又是那几个 Alice 簇，系统绕一圈回到原点。

根因是 v3 把两件不同的东西绑死了：**簇的身份（Alice 是谁）**和**簇的存储局部性
（Alice 这次出现在哪）**。拆开之后：cluster 只有一个 centroid，`K` 不因复现而增长；
segment 只标记时序段，用于约束合并（§5.11），**不各自分配存储**。

**副产品（值得单独写进论文）**：segment 就是"语义制导的连续段"，于是最早被降级的
"连续性约束 Ward 合并"方案作为**存储层**回来了，与语义路由层是组合关系而非竞争
关系。两条线在这里合流。

#### 时序约束为什么是前提而不是补丁

[DeltaKV](https://arxiv.org/abs/2602.08005) 实测**超过 60% 的语义相似 token 在位置上
相距 16 以上**。纯语义聚类下"成员时间上散得很开"是常态而非例外。没有时序约束，
entry 的 `(p_lo, p_hi)` 会例行覆盖大半个文档，位置信息退化成"这个簇全文都有"。

**注意约束必须作用在路由上，不能只靠 ladder 补救**：簇内成员按到达顺序进 ladder，
而到达顺序即位置顺序，所以 Fenwick 合并的本来就是"位置相邻的成员"。但如果成员本身
散布，即使 level-1 entry（只合并 2 个成员）跨度也可能上千。**唯一的杠杆在路由。**

**理论定位**：这正是 [distance-dependent CRP](https://arxiv.org/pdf/0910.1022)
（Blei & Frazier）——ddCRP 让落座概率同时依赖距离函数 `f(d_ij)`，把 `d` 取成时间
距离就得到本方案。这是 CRP 的推广而非替换，下面的预算故事完整保留。

#### `η` 是把两套方案连起来的连续旋钮（S0.0 的由来）

- `η → 0`：纯语义聚类，完全不管时序；
- `η → ∞`：每段都连续，**退化成"连续性约束 Ward 合并"的语义分段方案**。

所以扫 `η` 不是调参，而是在直接回答：**收益究竟来自"语义分组"本身，还是仅仅来自
"更好的分段边界"？** 先验上没有答案，且能纯 CPU 离线测出来——因此列为 **S0.0，
Stage 0 最高优先级**，排在内存问题（S0.6）之前，因为它决定的是立论而非工程可行性。

#### 簇内 ladder 是承重墙，不是装饰

如果只有一层平面簇，needle 一旦没被隔离、而是并入一个几千成员的大簇，就被稀释成
1/几千——比现在的 1/32 更糟。ladder 保证新近成员保持细粒度。与现状的一处语义变化：
**ladder 的 level 0 存单 token（`w=1`）**，不是像现有方案那样存"2 token 合并"。
这条改动是 §3 needle 论证和 §5.9"小簇无损"性质的承重点，不能省。

#### 读出端不感知簇/段结构

`log_kv_slot_attention` 只看到一个扁平槽池加一个有效位掩码。cluster 和 segment 都
只是写路径的路由元数据。这是保持张量矩形、避免 ragged 布局的关键设计（§5.7）。

#### 预算的理论形状

DP-means/ddCRP 是 CRP 混合模型的小方差极限，簇数天然满足

```
K_n = 1 + Σ_{i=1}^{n-1} Bern( α / (α+i) )      E[K_n] ≈ α·ln n
```

因为是独立 Bernoulli 之和，Chernoff 直接适用，超出常数倍期望的概率是 n 的多项式
小量——**高概率界**，不只是期望好看。最坏 `K=n`，且只在"每个 token 都远离所有已有
簇"（数据真的没有可压缩结构）时发生，退化是数据挣来的，不是运气差。

**必须提前接受的经验事实（未验证，S0.2 待测）**：自然语言的类型数按 **Heaps 定律**
`~ n^d`（d≈0.4–0.6）增长，不是 log n，对应 Pitman-Yor 而非 CRP。`K_eff` 大概率是
幂律而非对数——`O(√n)` 依然是合格的次线性目标（32k 下约 181 簇，仍远少于现在的约
2560 槽），论文里写 `O(n^d)` 带可调 d 比硬说 log n 更站得住，但这必须实测。

### 2.2 位置表示（v3 起）：真实锚点集，不做任何平均

**病因先钉死**：v1/v2 的 `z_f = Σ w_j e^{iθ_f p_j} / Σ w_j` 本质上是把多个振荡量叠加
求平均。只要成员位置散布，高频通道上相位就会在圆上转到四面八方而抵消——这是数学
事实，任何"求平均"式的位置统计量都逃不掉。v2 的 `log ρ` 置信项只是让模型**诚实地
知道**相消发生了，并没有阻止它发生。**要结构性杜绝相消，唯一的办法是不做平均。**

**定义**：每个 entry 存该 entry 覆盖范围内**真实成员**的位置锚点（整数，不是统计量）：

```
p_lo   = 该 entry 内最早成员的真实位置
p_hi   = 该 entry 内最晚成员的真实位置
p_mid  = "主要集中在哪"——权重更大一侧继承而来的某个真实成员位置
```

键仍存 **pre-RoPE 内容均值** `k̄_raw`（内容侧的均值没有振荡问题，不需要改）。

**读出**：对同一个 `k̄_raw` 在每个锚点上分别做**标准、未经任何修改的 `apply_rope`**：

```
k_eff_a = apply_rope( k̄_raw, cos_cache[p_a], sin_cache[p_a] )     a ∈ {lo, mid, hi}
```

每个虚拟槽用的都是**真实发生过的位置**，`ρ ≡ 1`，相消不可能发生。这比 v2 更彻底也
更简单：不需要复数 buffer、不需要 gain 公式、不需要 `κ`、不需要争论怎么汇总频率
——整套机制就是原封不动的 `apply_rope`，只是在几个真实坐标上各调用一次。
**"和 token 级 RoPE 一样优美"在这里是字面意义上的：它就是 token 级 RoPE。**

**合并规则**（比 z 的加权圆均值更干净的代数结构）：

```
p_lo  = min(A.p_lo, B.p_lo)
p_hi  = max(A.p_hi, B.p_hi)
p_mid = A.p_mid if A.w >= B.w else B.p_mid      # 永远是某个真实成员的真实位置
```

min/max 是精确、可结合、**幂等**的合并——比加权复数平均更贴合 Fenwick/segment tree
这类结构本来就偏好的幂等半格运算：合并顺序和层级都不影响最终值，零累积误差。

`p_mid` 不是精确中位数，但它永远是真实成员的真实位置，不是合成出来的。是否值得加
第三个锚点由 S0.6 决定（去重后的槽数膨胀倍数）。

### 2.3 打分接入：复用现有 kernel + 一处必须做的 mass bias 修正

每个 entry 展开成 1~3 个虚拟槽（去重后，`p_lo == p_hi` 的年轻 entry 自动只剩 1 个），
塞进现有的 `log_kv_slot_attention`：

```
score_{s,a} = scale·(q · k_eff_{s,a}) + λ·log( w_s / M_s )   ← 注意分母
value_{s,a} = v̄_s
```

**`/M_s` 是必须做的正确性修正，不是可选优化。** 如果一个 entry 展开成 `M_s` 个虚拟
槽而每个都带着完整的 `w_s`，`+λ·log(w_s)` 会被计入 `M_s` 次，softmax 里这个 entry
实际拿到的质量变成 `M_s · w_s · exp(score)` 而不是设计初衷的 `w_s · exp(score)`
——一个会静默放大大跨度簇权重的 bug。**这个坑是 v3 引入的，写实现时必须首先处理。**

不再需要 `κ`、不再需要 `log ρ` 置信项——没有任何虚拟槽在"撒谎"，每个都是它所在真实
位置上的诚实读出。v2 那一整条设计链在 v3 里被消解掉，不是被绕过。

**v3 相对 v2 的诚实代价对比**：

| 维度 | v2（z 统计量）| v3（锚点集）|
|---|---|---|
| 相消问题 | 诚实报告（`log ρ` 置信项）| **结构性不存在** |
| 存储/槽 | `rope_n_elem` 个 fp32（约 128 float）| 2~3 个整数 |
| 额外超参 | `κ`，需要 CPT 校准 | 无 |
| 命中"典型/中心"位置 | 好（圆均值是 MMSE 点估计）| 弱（只有离散锚点，中间空隙无代表）|
| 命中"边界"位置 | 弱（平均会模糊边界）| **精确**（边界就是真实成员）|
| 槽数开销 | ×1 | 去重后 ×1~3（**S0.6 必测**）|

核心的偏差-方差权衡：z 是"平滑但可能虚构"的中心估计，v3 是"精确但离散"的边界估计。
成员均匀散布的簇，查询若想对齐"跨度中间"的内容，z 平均出的相位可能刚好命中而 v3 的
边界锚点谁都对不上——这是真实存在、需要接受的短板。z 因此保留为 §8 的正式消融对照。

### 2.4 开放问题：均值池化无法表达 supersession（未定，S0.7 判定）

**问题是真的**：均值池化无法表达"后面的信息作废前面的"。"考了 100 分"和"更正为
90 分"平均成 95，是一个从未存在过的数。[Gated DeltaNet](https://arxiv.org/pdf/2412.06464)
的 `S_t = α_t (I − β_t k_t k_tᵀ) S_{t−1} + β_t k_t v_tᵀ` 里的 Householder 项干的正是
"先擦掉当前 key 方向上已有的关联、再写入新值"。

**但"每簇维护一个 gated delta state"暂不采纳**，三条理由（第 2 条决定性）：

1. **KV cache 不该替模型做遗忘决定。** DeltaNet 擦除是因为它的 state 是**固定大小**
   的。SemanticLogKV 的前提是"次线性但会增长"的内存，它**付得起**把 100 和 90 都以
   更粗的分辨率留着。擦除是在不知道未来 query 的情况下做的不可逆承诺——如果 query
   是"更正之前原本报的是多少"，答案已经被毁了。
2. **读出机制不兼容，且不兼容的方向恰好是捞针。** 线性 state 的读出 `qᵀS` 是所有
   写入的叠加，没有选择性——这正是线性注意力/Mamba 召回差的根因。更尖锐的是：
   **簇内 key 高度相似恰恰是线性 state 容量的最坏情况**（近平行 key 相互干涉最严重）。
   needle 若落进 haystack 大簇，均值池化下至少贡献 1/w，delta rule 下会被后续写入
   **彻底擦除**——代价从"稀释"（原则上可恢复）恶化成"抹除"（不可恢复）。
3. **训练路径代价。** delta state 要让梯度穿过 recurrence，是训练路径的重写，也是
   最容易引入训推不一致的地方（pin 一号死因）。

**已知边界（反例二）：segment 机制只解决远距离更正，不解决相邻更正。** "我考了
100 分——不对，是 90 分"这种紧挨着的更正中间没有被打断，照样进同一个 entry 被平均。
而自然文本里紧邻更正恐怕比隔了 5000 token 的更正更常见。缓解（不是解决）：相邻
token 在 recent window 里长期精确保留，真正合并时也是 level 0/1 的小 `w`。
**§2.1 的 segment 机制不能被宣传成"解决了 supersession"。**

**能保留这个洞察的落地方式（推荐路径）**：现有的 rank-1 Γ 修正**结构上已经是一个
秩 1 的线性 state**——`read_s = v̄_s + scale·γ_s·(q·γa_s)·γb_s` 就是 `qᵀ(γa γbᵀ)`。
所以诉求不需要新结构，只需改 `Γ` 的**构造方式**：从二阶矩改成 delta-rule 式更新
（后进成员在冲突方向上对先进成员有擦除权）。均值 `v̄` 作为不可擦除的底座保留、
softmax 选择性读出保留、改动局限在 `_pair_rank1_stats`/`compact` 内部。
**这条路径对相邻更正同样有效**，是目前唯一能覆盖反例二的方案。

**判定实验 S0.7**：统计簇内成员 value 是**互相并存**还是**系统性作废**，并**分别
统计相邻更正与远距离更正的占比**。**这个分歧靠实验解决，不靠辩论。**

### 2.5 悬而未决的根本性质疑：key 空间未必是语义空间

我们一直说"语义簇"，但实际聚类的是 **attention key 空间**，而 key 是"这个头做匹配
需要的东西"，不一定是主题语义。不同注意力头分工差异很大：有的做位置匹配，有的做
句法角色匹配，有的才做主题匹配。对一个句法头来说，"He/She/It"可能聚成一簇而跟它们
各自的指代对象分得很开——这时"簇有持续的话题身份、可以被打断和恢复"这个前提
**根本不成立**。

**层间也有同样问题**：早期层的 key 接近 token embedding 加位置，几乎没有主题结构，
聚类可能纯粹是词形层面的。

**但这也是自洽的**：per-head 聚类下，如果某个头的 key 本来就按位置组织，它的簇天然
时序连续，segment 规则自动变成 no-op，不造成伤害。所以这不是 bug，而是意味着
**整套方案的价值在不同头、不同层之间差异会很大**。

**操作后果（硬性要求）**：Stage 0 的所有统计**必须逐层 × 逐头分开报告**。一个"平均
看起来还行"的结果可能是少数几个中层语义头很好、多数头完全无效的叠加，而这两种情况
对应完全不同的下一步（前者应该考虑只对部分层/头启用语义模式，后者应该停止）。

## 3. 为什么能解决捞针

**稠密注意力**：`score = scale·(q·k_needle)`，`value = v_needle`。

**现有池化（needle 落在宽度 w 的槽里）**：

```
k_slot   = ( k_needle + Σ_{j≠n} k_j ) / w
q·k_slot = (q·k_needle)/w + (w-1)/w · (q·k̄_hay)
v_slot   = ( v_needle + Σ_{j≠n} v_j ) / w
```

分数侧和读出侧**同时**被稀释 w 倍。`+λ log w` 补的是计数质量而非内容，补不回稀释；
即使这个槽赢得注意力，返回的 `v_slot` 也已经是 haystack 均值，而 Γ 修正是 rank-1。
这解释了为什么二阶修正把 niah 从 0.032 提到 0.0827（2.6×）却依然离 0.9353 极远：
**它修的是分数，修不了读出。**

**SemanticLogKV（v3.1）**：needle 与所有 haystack centroid 距离 `> λ_new` ⇒ 自成
一簇 ⇒ 成员数 ≤ B′ ⇒ **ladder 永不填满 ⇒ entry 永不合并**（§5.9）；且
`p_lo = p_hi = p_mid` ⇒ 去重后 `M = 1`：

```
score = scale·(q · k_needle) + λ·log(1/1) = scale·(q · k_needle)   ← 与稠密逐位相同
value = v_needle                                                    ← 与稠密逐位相同
位置  = 在 p_needle 上的标准 RoPE                                    ← 与稠密逐位相同
```

**分数、读出、位置三者同时恢复到稠密**，这是"needle 自成簇"这个结构带来的，不是靠
调权重或加修正项凑出来的。而且这个论证**不依赖任何超参**（v1 要指定 β、v2 要指定
κ、v3 什么都不用指定）。

代价只有一个槽——haystack 语义高度同质、`K_eff` 小、压得很狠，省下的预算转移给异常
点。**这就是为什么自适应预算不是锦上添花而是唯一机制**：固定均匀预算在信息论上无法
同时做到"压缩 haystack"和"保留 needle"，因为它不允许预算转移。

**失败模式（必须在 Stage 0 证伪）**：`λ_new` 太大 → needle 并入 haystack 簇 → 退回
稀释。这是全方案最敏感的超参，S0.3 就是为它设计的。

## 4. 最坏情况的双重兜底

**表现的地板**：内容不可压缩时 `K` 增长 → 槽变多 → 质量向稠密平滑逼近。`K` 撞上
`K_max` 时强制 Ward 合并，把结构推回"少数大簇 + 位置序 ladder"，等价 `K_max=1`
锚点，即**今天的 LogKV 行为**。退化的终点是现状，不是比现状更差。

**性能的天花板**：硬性预分配上界。这里必须区分两笔不同的账（§12-C）：

| 口径 | vanilla | SemanticLogKV | 用途 |
|---|---:|---:|---|
| **持久 cache 内存**（entry 数）| 3584 | **2688** = 1024 + K16×B′8×L13 | memory-matched 公平性对比用这个 |
| 读出时槽池（entry × M，瞬时）| 3584 | ~3520（E[M]=1.5）| 计算量/峰值内存用这个 |
| 同上，最坏 E[M]=3 | 3584 | 6016 ✗ | **S0.6 的决策门** |

**锚点展开的 `M` 只影响读出时的瞬时槽池和分数张量，不影响持久化的 cache 内存**
——每个 entry 存的是 3 个整数，不是 3 份键值。之前的版本把这两笔账混在一起，
使内存故事看起来比实际差。

三层缓解手段（若 S0.6 显示 `E[M]` 逼近 3）：
1. 去重天然省钱——年轻/窄跨度 entry 自动收敛成 1 个锚点；
2. 多锚点只用在**粗粒度 entry**（老的、宽跨度的），年轻 entry 保持单锚点；
3. 调低 `K_max`/`B′` 把总槽数摁回对齐基线。

> **论文层面最大的攻击面**：自适应预算意味着 niah 变好可能仅仅因为多用了内存——
> 而 niah 恰恰最容易把 K 推高。**从第一个实验开始就必须报告每个 benchmark 上的
> 实际占用（两笔账都报）**，并准备 memory-matched 对照。这一条如果留到最后补，
> 整套实验要重跑。

## 5. 算法规格与实现方案

### 5.1 记号与参数

> **记号冲突警告**：早期版本用 `λ` 同时表示 mass bias 系数和 segment 阈值，用 `τ`
> 表示 cosine 阈值。现已统一如下，`λ` **只**保留给现有代码已占用的 mass bias。

| 符号 | 参数名 | 默认 | 含义 |
|---|---|---|---|
| — | `log_kv_semantic_clusters` | `false` | 总开关，关闭时逐字节复现改动前行为 |
| `K_max` | `log_kv_cluster_k_max` | `16` | 簇数硬顶 = 性能天花板 |
| `B′` | `log_kv_cluster_entries` | `8` | ladder 每级 entry 数；同时是**无损阈值**（§5.9）|
| `L_max` | 由 `max_seq_length` 推导 | `13` | `⌈log₂(N/B′)⌉`，不是自由参数 |
| `λ_rel` | `log_kv_cluster_lambda_rel` | `1.0` | 新簇距离阈值（相对量，见 §5.2）|
| `η` | `log_kv_seg_eta` | `1.0` | join cost 时序权重。`0`=纯语义，`∞`=纯连续分段（**S0.0 主扫描**）|
| `g0` | `log_kv_seg_g0` | `2048` | 时序项的饱和尺度 |
| `g_max` | `log_kv_seg_gap_max` | `4096` | 开新 segment 的间隔阈值 |
| `γ` | `log_kv_seg_forget` | `0.5` | 跨段的 centroid 计数遗忘因子（§5.5）|
| `λ` | `log_kv_lambda`（现有）| `1.0` | **mass bias 系数**，沿用现有语义 |

沿用项目惯例：`demo.py`/`eval.py` 走 `run_cli()` 签名自省与 `_o()`，**YAML 新字段
默认必须写 `null`**（否则会被同名 CLI 参数静默覆盖，见 §11），不进 `eval.sh`/
`majob.sh` 核心列表，走 `DIAG_ARGS` opt-in。

### 5.2 分簇：度量与阈值

**用原始（未归一化）pre-RoPE key 上的平方欧氏距离，不用 cosine。** 理由不是习惯，
而是这一个量同时是三个东西：

- 槽内池化误差对分数的影响是 `q·(k_i − k̄)`，所以**控制误差的量就是欧氏距离**；
- DP-means 的分配准则 `‖k − μ_c‖² > λ_new`；
- Ward 合并代价 `(n_a n_b)/(n_a+n_b)·‖μ_a − μ_b‖²`，即簇内平方和的增量，也正是
  现有 `_pair_rank1_stats` 已经在算的量。

**一个度量贯穿路由、合并、误差分析三处**，不需要在 cosine 和 L2 之间来回翻译。
副作用：attention sink（巨范数 token）因为距离所有 centroid 都远而自动隔离——正是
想要的行为（对比 6.5 的重要性池化是把 sink 加权**放大**，方向相反）。

**阈值必须相对化，否则跨层不可比**：`k` 的范数在不同层、不同头之间差好几个数量级，
单一全局 `λ_new` 必然在一半的头上失效。

```
λ_new = λ_rel · s_h        s_h = 该 (layer, head) 上 E‖k − k̄‖² 的滑动估计
```

`s_h` 用 recent window 里现成的 token 在线估计，零额外前向。**这一条如果不做，
S0.2 扫出来的 `K_eff` 曲线在层间完全没有可比性**，整个 Stage 0 会得出无意义的结论。

### 5.3 分簇：三路判定

有两个**性质不同**的决策——"这内容新不新"（花一个 centroid 槽）和"时序断没断"
（花 ladder 局部性）。硬揉成一个阈值就是重犯 v3 的错。正确结构是：
**统一代价用于挑候选，两个阈值用于两个决策。**

```
对每个 flush 出来的 token x（绝对位置 p）:

  d_c = ‖k_x − μ_c‖²  +  η · φ(p − p_hi_c)        # 统一代价，仅用于排序
        φ(g) = g / (g + g0)                        # 有界饱和：时序项不能无限支配
  c* = argmin_c d_c

  if   ‖k_x − μ_{c*}‖² > λ_new :   → 开新 cluster              # 语义 novelty
  elif p − p_hi_{c*}   > g_max  :   → 同 cluster，开新 segment   # 时序被打断
  else:                              → 同 cluster，当前 segment
```

时序项在这里的作用是**平局裁决**：两个簇语义上差不多近时，优先给最近活跃的那个。
`η → 0` 退化成纯语义，`η → ∞` 退化成纯连续分段——S0.0 扫的就是它。

`φ` 必须有界饱和，否则一个很老的簇会被赋予近乎无穷的惩罚，等价于硬性禁止回访，
那就退回 v3 的 fork 行为了。

### 5.4 分簇：批量化路由（三阶段）

朴素实现要逐 token 串行（centroid 边分配边更新），代价是单次前向 45 万次 Python
迭代，训练完全不可行（§12-B）。解法基于一个频率观察：**"加入已有簇"是常态，
"开新簇"是罕见事件**。

```
Phase 1（并行，覆盖绝大多数 token）:
    冻结 centroid，一次 einsum 算出 (m, K) 距离矩阵
    所有 min_c ‖k−μ_c‖² ≤ λ_new 的 token 直接按 §5.3 分配到位

Phase 2（串行，只处理 orphan）:
    min_c ‖·‖² > λ_new 的 token 需要开新簇，它们之间还可能互相成簇
    在这批 orphan 内部跑一个小 DP-means（O(m_orphan²) 的距离矩阵即可）

Phase 3:
    批末统一更新 centroid 一次（§5.5）
```

**摊还论证**：Phase 2 的总执行次数被"新簇事件总数"界住，即 `E[K] ≈ α ln n`（或
Heaps 下的 `n^d`），**不是 `O(T)`**。所以串行部分的总代价是 `O(K)` 而非 `O(T)`，
flush 粒度因此可以从 2 提到 128，`_flush_pairs` 那套批量化的收益完整保住。

**这是一个近似**（批内冻结 centroid）。**必须测它与严格串行版的分歧率**——列为
Stage 0 的 S0.8，纯 CPU，拿 dump 的 k 跑两遍对比即可。若分歧率高，说明 centroid
更新很敏感，那 §5.5 的遗忘因子 `γ` 会加剧问题，需要重新权衡。

### 5.5 分簇：centroid 更新

段内话题稳定 ⇒ 计数均值是对的；跨段可能已漂移 ⇒ 新成员该占更大权重。一个参数：

```
段内:      μ_c ← μ_c + (k − μ_c) / n_c ,   n_c ← n_c + 1
开新段时:  n_c ← γ · n_c                    # γ=1 纯均值，γ=0 直接替换
```

这就是 §2.4 里说的"零副作用的门控更新"——centroid 只是路由元数据，不参与 attention
读出，擦它不丢任何信息，所以这里没有"cache 替模型做遗忘决定"的顾虑。

### 5.6 分簇：`K_max` 满时的 Ward 合并

选 Ward 代价 `(n_a n_b)/(n_a+n_b)·‖μ_a − μ_b‖²` 最小的两个簇合并。合并两条 ladder
时利用**两者都是 Fenwick 形状、同一 level 的 entry 宽度相同**这个性质：

```
for ℓ in 0 .. L_max:
    把 a、b 在 level ℓ 的 entry 拼起来（≤ 2B′ 个，按到达顺序归并）
    保留 B′ 个，其余成对合并进位到 ℓ+1        # 就是 _binary_carry 的级联
```

`O(L·B′)` 一次，且合并事件本身罕见（次数被超出 `K_max` 的新簇事件数界住）。这是
现有 `_binary_carry` 的自然推广，不需要新机制。

### 5.7 簇内压缩：ladder 结构

```
level 0:  B′ 个 entry，每个 w = 1        ← 单 token，不是现在的 2-token 合并
level ℓ:  B′ 个 entry，每个 w = 2^ℓ
level ℓ 满（B′ 个）时，成对合并进位到 ℓ+1
```

因为路由发生在 flush 时、按到达顺序，**簇内成员序列就是位置序列**，所以 Fenwick
合并的天然是位置相邻的成员——这正是锚点跨度需要的。

### 5.8 簇内压缩：O(log n) 保证的准确陈述

单簇容纳 `n_c` 个 token 需要 `L_c = ⌈log₂(n_c/B′ + 1)⌉` 层，占 `B′·L_c` 个 entry。
求和：

```
Σ_c B′·log₂(n_c/B′)  ≤  K·B′·log₂( n / (K·B′) )  =  O( K·B′·log n )
```

不等号来自 log 的**凹性**：给定总 token 数，簇均衡分布时求和最大。所以
**最坏情况是簇均衡**，一个簇吃掉全部反而更省——这个界是安全的，不需要额外假设。

`L_max = ⌈log₂(N/B′)⌉` 由最大序列长度定死，不是自由参数。

### 5.9 簇内压缩：小簇是无损的（needle 论证的承重点）

level 0 存单 token 意味着，**成员数 ≤ B′ 的簇，它的每个成员都以精确形式存着，
从不合并**。这不是巧合，是"预算转移"的具体兑现：

- needle、罕见实体、异常内容 → 小簇 → **精确保留**（分数、读出、位置全部等同稠密）
- haystack、模板文字、高冗余内容 → 大簇 → 对数压缩

压缩率因此是**内容自适应**的，而不是像位置分桶那样按距离一刀切。§3 那个"needle
恢复到稠密"的论证，承重的就是这条性质，而它依赖 level 0 存单 token 这个改动。

### 5.10 簇内压缩：合并算子

```
merge(A, B):                          # B 在到达顺序上更晚
    w    = w_A + w_B
    k̄    = (w_A·k̄_A + w_B·k̄_B) / w     # pre-RoPE 内容空间
    v̄    = (w_A·v̄_A + w_B·v̄_B) / w
    p_lo = min(A.p_lo, B.p_lo)          # 锚点：精确、幂等（§2.2）
    p_hi = max(A.p_hi, B.p_hi)
    p_mid = A.p_mid if w_A ≥ w_B else B.p_mid
    Σ, Γ : 现有 Chan-style 二阶矩合并 + rank-1 截断（唯一有损的一步）
```

`O(d)`，与现有 `compact()` 完全同构——**Σ/Γ 那套一行都不用改**，只是统计空间从
post-RoPE 换成 pre-RoPE。**顺带的好处**：Σ 不再混入位置相位方差，簇内同质时 rank-1
近似精度应系统性恢复——这是另一分支 D1 自检里 width≥8 失真的根因之一。

### 5.11 簇内压缩：跨 segment 的进位

segment 在簇内时序有序，而 ladder 合并到达相邻的 entry，所以**跨段合并只可能发生在
段边界上，每层每边界至多一次**。宽跨度 entry 的数量因此被段边界数界住，不是被 entry
总数界住。

**建议允许这种合并**（锚点如实记录宽跨度），而不是阻断——阻断需要填充对齐、白白
浪费槽位，而损害本来就有界。S0.5 测的就是这个损害有多大。

**这条设计同时解决了 §12-C 的预分配爆炸**：segment **不各自分配 ladder**，每个
cluster 只有一条，所以预分配是 `K_max × B′ × L_max`，不含 `SEG_max` 因子。

### 5.12 预算定尺

```
L_max  = ⌈log₂(N / B′)⌉                  # 由 max_seq_length 定死
K_max × B′ = 真正的预算旋钮
```

`K` 与 `B′` 的分配比例是个权衡：**`K` 大 = 语义分辨率高**（更多话题各自成簇），
**`B′` 大 = 时序分辨率高**（簇内保留更多细粒度 entry，且 §5.9 的无损阈值更高）。
这个比例应该由 **S0.2/S0.3 的结果反过来定**：实测 `K_eff ≈ 50` 说明 `K_max=16`
太小、簇会被迫强行合并；`K_eff ≈ 8` 则应把预算挪给 `B′`。

参考配置（与 vanilla 的 3584 槽对齐）：`K_max=16, B′=8, L_max=13` → 1664 entry +
recent 1024 = **2688**；单簇最多可覆盖 `B′·(2^L − 1) = 8·8191 ≈ 65k` token，32k 下
即使全部落进一个簇也不会溢出。

### 5.13 新模块 `litgpt/log_kv_position.py`

无状态、无可学参数、CPU 可测。v3 起不再需要复数统计量、gain 公式、置信度函数。

```python
# 锚点合并：精确、可结合、幂等
def merge_anchors(lo1, hi1, mid1, w1, lo2, hi2, mid2, w2):
    lo  = torch.minimum(lo1, lo2)
    hi  = torch.maximum(hi1, hi2)
    mid = torch.where(w1 >= w2, mid1, mid2)   # 永远是某个真实成员的真实位置
    return lo, hi, mid


# 锚点 -> 虚拟槽键：就是标准 RoPE，在真实位置上取现成的 cos/sin cache 行
def materialize_anchor_keys(k_raw, anchors, cos_cache, sin_cache, rope_n_elem):
    """anchors: (..., S, M) int64 真实位置; 返回 (..., S, M, d)
    每个锚点一次 apply_rope —— rho 恒为 1，不存在相消。"""
    cos = cos_cache[anchors]        # (..., S, M, rope_n_elem)
    sin = sin_cache[anchors]
    x   = k_raw.unsqueeze(-2)[..., :rope_n_elem]
    h   = rope_n_elem // 2
    rot = torch.cat((-x[..., h:], x[..., :h]), -1)
    roped = x * cos + rot * sin
    tail  = k_raw.unsqueeze(-2)[..., rope_n_elem:].expand(*roped.shape[:-1], -1)
    return torch.cat([roped, tail], -1)


# 去重 + mass bias 摊薄因子
def dedup_anchors(lo, hi, mid):
    """返回 (unique_anchors, M) —— p_lo==p_hi 的年轻 entry 自动收敛为 M=1。
    调用方必须用 log(w / M) 而不是 log(w) 做 mass bias（§2.3）。"""
    ...
```

**单测（S0.1，纯 CPU，不依赖 dump）**：
- **嵌套精确性**：单 token entry（`p_lo=p_hi=p_mid`）→ 去重后 M=1，输出与
  `model.apply_rope` 在该位置上逐位一致。
- **合并的幂等性与结合律**：任意二叉合并顺序结果一致；`merge(A,A) == A`。
- **锚点永远真实**：随机构造合并树，断言每个输出锚点都属于原始成员位置集合。
- **mass bias 守恒**：M 个虚拟槽在 `λ·log(w/M)` 下的 softmax 质量加总，等于单槽在
  `λ·log(w)` 下的质量（§2.3 那个 bug 的回归测试，**必须有**）。
- **v2 对照**：保留 z 统计量实现与其 Dirichlet 闭式解单测，供 §8 消融使用。

### 5.14 `litgpt/log_kv_cache.py`

- 新增 buffer：每 entry `(p_lo, p_hi, p_mid)` int32、每 entry `segment_id` int16；
  每 cluster `centroid`（`(B,G,K_max,d)`）、`n_c`、`p_hi_c`；每 (layer,head) 的
  尺度估计 `s_h`。
- 键改存 **pre-RoPE**。recent window 同样存 pre-RoPE + 位置，读出时统一走
  `materialize_anchor_keys`——**一条代码路径**，保证嵌套性质在生产里被真实执行。
- `compact()`/`_binary_carry()` 增加锚点合并（一行 `merge_anchors`），Σ/Γ 改在
  pre-RoPE 内容空间统计（§5.10）。
- 路由 `_route()`：按 §5.2–§5.6 实现，`@torch.no_grad()`，与 cache 更新同路径。
  **必须同时把路由决策写进一个 `(B,G,T)` int16 buffer 供 backward 重放**（§12-A，
  这是硬性要求，不是优化）。
- `get_attention_state()` 额外返回 `(B,G,S)` 有效位掩码与每 entry 的 `M_s`；
  `log_kv_slot_attention` 增加可选槽有效性掩码参数（fp32 分数上填 `-inf`），与现有
  `causal_tail` 正交；**mass bias 改用 `λ·log(w_s / M_s)`**。

### 5.15 `litgpt/model.py`

唯一的接口性改动：cache 现在需要 **pre-RoPE 的 k** 和该 token 的**绝对位置索引**。
模型本来就持有这两样，所以是传参改动而非新计算。`build_log_kv_cache`/
`set_log_kv_cache`/`enable_log_kv_training` 三处透传。读出侧需要访问 RoPE cache
做锚点物化。

### 5.16 矩形张量与 batching

- **按 (B, G) 独立聚类**——不同头关注不同语义，共享簇是错的（§2.5）。
- **预分配 + 掩码**：entry 张量固定为 `K_max × L_max × B′`（**不含 `SEG_max`**，
  见 §5.11），占用不满时掩码屏蔽。张量始终矩形，不引入 ragged 布局。
- 读出时的虚拟槽池为 `entry 数 × M_max`，是瞬时张量，不是持久 cache（§4）。
- ragged/paged 布局留到工程化阶段，不在研究验证期做。

## 6. 实验协议

四个阶段，每阶段带可证伪的决策门。顺序不能打乱：Stage 0 几乎不花 GPU，却能在写任何
生产代码之前否掉整个方案。

> **贯穿性硬要求（§2.5）**：Stage 0 的所有统计**必须逐层 × 逐头分开报告**。聚合
> 数字会把"少数语义头很好 + 多数头无效"和"所有头中等"混为一谈，而这两种情况对应
> 完全不同的下一步决策。

### Stage 0 — 离线可证伪（1 次 GPU dump + 全部 CPU 分析）

只需要一次前向：对几条 32k 的 NIAH prompt dump 每层每头的 **pre-RoPE k / v** 与
needle 的 token span（复用另一分支已有的 `log_kv_pin_diag.py` 定位逻辑）。之后所有
分析在 CPU 上做。

| 编号 | 测什么 | 为什么 |
|---|---|---|
| **S0.0** | **扫 `η`：0（纯语义）→ ∞（纯连续分段），看槽内内容方差与锚点跨度的联合曲线** | **全课题最根本的问题：收益来自语义分组本身，还是仅仅来自更好的分段边界？** |
| S0.1 | §5.13 单测（含 mass bias 守恒回归测试）| 数学正确性，纯 CPU，不需要 dump |
| S0.2 | `K_eff(n)` 与 segment 总数随 `λ_rel`、`η` 的曲线，拟合 log/幂律/线性 | 预算故事成不成立；反过来定 `K:B′` 的分配（§5.12）|
| S0.3 | needle 隔离率：所在簇的成员数分布（关键是 `≤ B′` 的比例）| §3 + §5.9 的核心机制成不成立 |
| S0.4 | 簇内 **key 方差**与 **value 方差**（两个都要）/ 现有位置槽内方差 | key 方差管分数侧，**value 方差管读出侧**，只测前者会高估收益（§12-D）|
| S0.5 | entry 的 `(p_hi − p_lo)` 跨度分布，随 `η` 变化 | 验证时序项确实压住了跨度 |
| S0.6 | 锚点去重后的平均倍数 `E[M]` 与分布 | 读出槽池会不会失控（§4）|
| S0.7 | 簇内 value 的并存 vs 作废比例，**分相邻/远距离统计** | §2.4 开放问题的判定实验 |
| S0.8 | 批量化路由（§5.4）与严格串行版的分歧率 | 那个近似能不能用 |

**决策门**：
- **S0.0**：如果 `η→∞`（纯连续分段）已经拿到大部分收益，那么语义聚类这条线的边际
  价值有限，应当直接转向更简单的"语义分段"方案（工程量小一个量级、不需要 CPT）。
  **这个门开在最前面，就是为了避免在错误的复杂度上投入。**
- **S0.2**：32k 下 `K_eff` 应在 10² 量级。若达 10³ 以上，预算故事垮掉——回去把粒度
  调粗再谈。
- **S0.3**：needle 落在成员数 `≤ B′` 的簇里的比例应显著高于随机基线。若 needle 大多
  并入大簇，§3 的机制不成立，方案应就地停止，不要硬着头皮训。
- **S0.4**：key 方差应显著低于现有位置槽；**若 value 方差没有同步下降**，说明读出侧
  仍是 smear，收益要打对折，需要考虑按 `[k;v]` 联合聚类或簇内二次分裂。
- **S0.5**：`η` 增大时跨度中位数应显著下降。若无变化，说明时序项没起作用。
- **S0.6**：`E[M]` 应 < 1.6。若接近 3，必须走 §4 的缓解手段。
- **S0.7**：若簇内 value 系统性作废的比例 > 30%，把 Γ 的 delta-rule 广义化提到
  Stage 1 范围内；否则记录结论并搁置 §2.4。
- **S0.8**：分歧率应 < 5%。若更高，flush 粒度要调小，或 `γ` 要更保守。

### Stage 1 — 实现

按 §5 落地，默认关闭时逐字节复现现状。额外必测：`K_max=1` 且路由永远选中同一簇
（退化成单一位置序 ladder）时，数值上应逼近现有位置分桶实现。**动手前先读 §12。**

### Stage 2 — eval-time 探测

诚实的预期：新表示对现有 CPT 权重是分布外的，绝对分数可能不好看。价值在于**形状而非
绝对值**，Config A 是硬性正确性闸门。

| 配置 | 参数 | 期望 |
|---|---|---|
| A 正确性闸门 | `K=1`（强制单簇）| ≈ 0.1716 / 0.0827。**对不上就是实现有 bug，不要往下走** |
| B 纯语义 | `K=16, η=0` | 隔离时序项的贡献 |
| C 主实验 | `K=16`，扫 `λ_rel` 和 `η` | niah 应显著高于 A |

```bash
DIAG_ARGS="--log_kv_semantic_clusters true --log_kv_cluster_k_max 16 \
  --log_kv_cluster_lambda_rel 1.0 --log_kv_seg_eta 1.0" \
    bash majob.sh exp/qwen1.7b-32k/arc_warmup.yaml
```

沿用另一分支 §8.5 记录的坑：主 benchmark 参数不要传 `none`（LongBench 来自主列表）；
多配置串行跑（输出目录不按超参分开，靠 JSON 内的 metadata 区分）；新参数需要一并
写进 eval 的 metadata 字段。

### Stage 3 — CPT

Stage 2 有信号后再投入。v3 没有需要 warmup 的新标量（v2 的 `κ` 已被消解），训练侧
风险低于 v2；若 S0.7 触发了 Γ 广义化，那部分沿用 `second_order_scale` 的爬坡机制
（目标值别设太激进、warmup 别只给 10 步——另一分支 §4 真实踩过的崩溃教训）。

## 7. 消融表

每一行回答一个独立问题，不是穷举网格。所有行都必须附带**两笔内存账**（§4）。

| 旋钮 | 取值 | 回答的问题 |
|---|---|---|
| **`η`（时序权重）** | **0 / 0.3 / 1 / 3 / ∞** | **语义分组 vs 分段边界，谁贡献大？（S0.0 的 eval 版）** |
| `K_max` | 1 / 4 / 16 / 64 | 语义分组本身值多少分？1 是现状锚点 |
| `K:B′` 分配 | 32×4 / 16×8 / 8×16 | 语义分辨率 vs 时序分辨率，总预算固定 |
| `anchor_mode` | `lo_hi_mid` / `lo_hi` / `mid` / `z` | 锚点表示 vs v2 的 z 统计量 |
| `λ_rel` | 0.5 – 2.0 | needle 隔离与簇纯度的平衡点 |
| `λ`（mass bias）| 0 / 1 | 大簇的计数质量补偿是否仍然正确 |
| `γ`（遗忘因子）| 0 / 0.5 / 1 | centroid 门控更新值不值 |
| rank-1 Σ/Γ | 关 / 现有构造 / delta-rule 构造 | 第三档取决于 S0.7 |
| vanilla memory-matched | B 调大到同 entry 数 | **排除"只是多用了内存"** |

指标沿用现有四项（ACC/LongBench/LongBench_e/niah@32768），**另加 multi-needle**——
单 needle 一旦从 0.08 提上去就会迅速失去区分度，multi-needle 才是这类分层压缩方法
真正拉开差距、也最被审稿人看重的实验。v3 的锚点表示对 multi-needle 应有额外优势
（多个 needle 各自成簇、各自保留精确位置），这是值得单独讲的故事。

## 8. 风险与对策

| 风险 | 后果 | 对策 |
|---|---|---|
| **key 空间未必是语义空间**（§2.5，最根本）| 对句法头/位置头，"簇有持续话题身份"的前提不成立 | per-head 聚类下自动退化为 no-op；但 Stage 0 必须逐层逐头统计 |
| **`η` 选错** | 要么退回纯语义（跨度失控），要么退回纯分段（语义分组白做）| S0.0 是最高优先级实验，先测再定 |
| **`λ_rel` 敏感** | needle 并入大簇，机制失效 | S0.3 提前证伪；进主扫描 |
| **按 key 聚类不保证 value 同质**（§12-D）| 分数侧准了但读出侧仍 smear，收益打对折 | S0.4 必须同时测 value 方差 |
| **锚点展开撑爆读出槽池** | 计算量和峰值内存超基线 | S0.6 先测；缓解见 §4 三层手段 |
| **mass bias 未按 M 摊薄**（静默 bug）| 大跨度簇权重被放大 M 倍 | §5.13 的守恒单测是硬性 gate |
| **相邻更正仍被均值池化**（反例二，§2.4）| supersession 只解决了远距离那一半 | 已知边界，如实记录；唯一覆盖路径是 Γ 的 delta-rule 广义化 |
| **锚点之间的"空隙"无代表** | 成员均匀散布的簇，查询对齐中段时无锚点可命中 | 已知短板（§2.3），`p_mid` 部分缓解；z 作为消融对照量化差距 |
| **centroid drift / 冷启动**（§12-F）| 早期 token 定义了整篇文档的簇 | `γ` 遗忘因子缓解漂移；冷启动待定 |
| **内存不对齐比较** | 论文被质疑"只是多用了内存" | 见 §4，从第一个实验起报告两笔账 + memory-matched 对照 |
| **训练期显存随内容波动** | batch 内簇占用不同 | 预分配上界固定，训练期必须开硬顶 |

## 9. 相关工作定位（2026-08 快照，务必用 search 复核最新情况）

| 工作 | 做了什么 | 与本方案的区别 |
|---|---|---|
| SemantiCache (2603.14303) | 按分隔符切语义块 + 贪心种子聚类合并 | **先分段再聚类**；本方案**先聚类再分段**。且它固定 budget、无簇内分辨率层级、frozen model |
| Multipole Attention (NeurIPS 2025, 2506.13059) | key 做 k-means，远处用 centroid 近似打分 | **保留全量 KV（O(n) 显存）**，centroid 只是索引，不是压缩 |
| SeKV (2606.31145) | entropy 引导语义 span，GPU summary + CPU SVD 重建 | offload + 检索系统，什么都不丢，非次线性 |
| ClusterAttn (ACL 2025) | 密度聚类自适应簇数 | 稀疏注意力，固定 1024 token 预算 |
| GVote (ICLR 2026, 2509.03136) | 蒙特卡洛采样未来 query 免手工设预算 | eviction/selection 框架，预算仍是 O(1) 固定池 |
| DeltaKV (2602.08005) | 残差编码 KV，利用长程 token 相似性 | 不是次线性；**但它实测的"60% 相似 token 相距 >16 位置"是 §2.1 时序约束的实证依据** |
| Gated DeltaNet (2412.06464) / GDN-2 (2605.22791) | 门控 + delta rule 的固定大小线性 state | 固定容量下的覆盖式记忆；§2.4 详述为何不直接采用 |
| ddCRP (0910.1022) | 距离依赖的 CRP，落座概率含距离函数 | §5.3 join cost 的理论出处 |

空着的生态位：**非参先验决定簇数 + 严格次线性空间 + 语义身份与时序局部性两层分离 +
不做平均的真实锚点位置表示**。CRP/ddCRP 的集中不等式给了这条线一个别人写不出的
理论节。多次搜索没有找到任何工作用非参贝叶斯给簇数一个先验、同时保持严格次线性显存。

## 10. 复用另一分支的基础设施与教训（精简版）

只保留对本分支**操作上仍然生效**的结论（完整版在 `claude/semantic-cluster-log-
compression-8ai32a` 分支的 CLAUDE.md）。

**RoPE 布局约定**：`apply_rope`（`litgpt/model.py:2074`）是"前后半重复"布局，
`cos[f]==cos[f+d/2]`，partial rotary 只旋转前 `rope_n_elem` 维、尾部内容通道不旋转。
§5.13 的锚点物化直接依赖这个约定。

**pin 系列四条死因（设计规避清单，本方案逐条对照）**：
1. 训推不一致——pin 选择只在推理期跑，训练从未见过。本方案：路由同样 `no_grad`，
   但训练和推理走**同一条** cache 更新路径。**注意 §12-A 和 §12-E 是这条的新变体，
   必须显式处理，不能靠"设计上应该一致"来假设。**
2. 分数尺度失配——pin 槽是精确 key，混进池化槽的 softmax 行时点积尺度不同。
   本方案：槽种群完全同质（都是池化槽，包括单点簇）。
3. 观察窗口的架构性局限——pin 依赖一次性 dense prefill 和"prompt 尾部是问题"两个
   假设。本方案：路由在 flush 时逐 token 发生，不需要看到整段 prompt。
4. 剂量效应——pin 插得越多伤害越大。本方案：不新增槽种类，只改变成员划分。
   锚点展开形式上像"插入更多槽"，但它们共享同一个 `v̄` 和被摊薄的 mass bias，
   不是独立的新信息源，与 pin 的剂量效应不同源。

**工程习惯**：
- `eval.sh` 把 YAML 展平成纯 CLI flag（不传 `--config`），`_o()` 的"YAML 非 null
  覆盖 CLI"逻辑不会触发；直接用 `--config <yaml>` 会触发。新增会被扫参覆盖的字段，
  YAML 里必须写 `null`。
- `majob.sh` 如果 `save_path` 下已有 checkpoint 会跳过训练直接 eval——新实验必须用
  独立 `save_path`，不要复用另一分支的目录。
- 本地跑 pytest 用 conda env `mineru`（`/Users/hourunli/anaconda3/envs/mineru`，
  Python 3.12）；系统自带 Python 无法解析仓库里到处用的 `X | None` 类型注解。

## 11. 技术难点清单（动手写代码前必读）

按"会不会挡住你"排序。**A、B 必须在写第一行代码之前把方案定死**，事后补极痛。

### A. 训练侧重放确定性——语义路由打破了它赖以成立的前提（最危险）

`LogKVStreamTrainingAttention` 的 backward **不保存图，而是重置 cache 并重放整条流**。
它的正确性论证明确写在 docstring 里：

> "The replay is deterministic: compaction is pure mean-pooling with **count-based
> binary carries** — no RNG, identical shapes take identical kernels"

也就是说现有 cache 结构**只依赖计数，完全不依赖数据**，所以重放必然重建逐位相同的
状态。语义路由恰恰把结构变成数据依赖：一次 `argmin` 平局、一次距离落在 `λ_new`
边界上的浮点末位差异（TF32、einsum 归约顺序、autograd 开启与否导致的 kernel 选择
不同），就会让某个 token 进了另一个簇——**而一个路由决策的分歧会沿整条流放大，
最终梯度属于一个和 forward 输出不同的函数**。这正是 docstring 花大段篇幅防的事，
而且不会报错，只会让 loss 曲线莫名其妙。

**修法（硬性要求）**：forward 时把路由决策写进 `(B,G,T)` 的 int16 buffer，backward
直接**重放这个 id 序列而不是重算**。`O(T)` 整数，相对其它开销可忽略。把"靠浮点
可复现"换成"构造上精确"。

### B. 路由的顺序依赖——wall-clock 会直接爆炸，且有前车之鉴

`_flush_pairs` 存在的**唯一理由**就是把原本逐对串行的 flush 批量化。语义路由等于
把刚被消除的串行依赖请回来：32k / 2 = 16k 次 flush × 28 层 = **单次前向 45 万次
Python 迭代**，光 kernel launch 开销就是几十秒量级。

**修法**：§5.4 的三阶段批量化。核心是 Phase 2（串行）的总次数被 `E[K]` 界住而非
`O(T)`。**代价是一个近似，必须由 S0.8 验证。**

### C. `SEG_max` 曾把预分配乘爆（已在 §5.11 修掉，记录以免复发）

v3.1 早期版本把 §5.16 的预分配写成 `K_max × SEG_max × L_max × B′ × M_max` =
16×8×13×8×3 ≈ **4 万槽**，比 vanilla 的 3584 大一个数量级。根因是误以为每个 segment
需要自己的 ladder。**正解见 §5.11**：每 cluster 只有一条 ladder，segment 只约束合并；
跨段合并只发生在段边界，损害被段边界数界住。预分配回到 `K_max × B′ × L_max`。

同时记住 §4 那两笔账要分开：**锚点展开的 `M` 只影响读出时的瞬时槽池，不影响持久
cache 内存**（每 entry 存 3 个整数，不是 3 份键值）。

### D. 按 key 聚类只同质化了分数侧，没同质化读出侧（可能砍掉一半收益）

我们聚类的是 `k`，但 attention 返回的是 `v̄`。**key 相似不蕴含 value 相似**——
"分数是 X"这个 key 模式下，value 可以是 100 也可以是 90（这正是 §2.4 那个例子的
另一面）。于是：分数侧簇内 key 同质 ⇒ `k̄_raw` 准 ⇒ §3 成立 ✓；读出侧簇内 value
可能依然是一锅粥 ⇒ `v̄` 仍是 smear，而 Γ 是 rank-1 ✗。

典型症状是"niah 分数上去了但答案文本是错的"。**S0.4 必须同时测 key 方差和 value
方差**——纯 CPU、零额外成本，但决定收益是全额还是对折。若 value 方差居高不下，
可选项是按 `[k; v]` 联合聚类（会伤路由质量）或簇内按 v 二次分裂。

### E. flush 时机若依赖 chunk 边界 → 静默的训推不一致

路由顺序由 flush 调度决定。若 flush 只由窗口占用触发（与 chunk 无关），调度就是
token 索引的确定函数，训推一致；但只要有任何一处依赖 chunk 边界，`train_block` 不同
或 prefill 分块不同就会产生**不同的簇划分**。这是 pin 一号死因的变体，且多了一层
数据依赖，更难查。**必须显式验证，不能假设。**

### F. 冷启动：最初若干 token 定义了整篇文档的簇

DP-means 顺序依赖，而文档开头往往是 system prompt 或样板文字，同时 attention sink
（巨范数 token）也最先出现。用它们播种 centroid，等于让最不具代表性的内容决定整篇
文档的语义划分。可选缓解：用窗口内已有内容播种而非最初 K 个 token；或前若干 token
用更激进的 centroid 更新率。**尚未定案。**

### G. 压缩函数永远不会被训练——架构性限制，不是 bug

梯度只经由 token 自己所在的 block 到达（cache 提交的是 detached 副本），一旦被压进
槽就再也没有梯度。所以**聚类质量学不出来**，CPT 只能教模型去适应一个固定的启发式
划分，不能教它划分得更好。两个后果：`λ_rel`/`η` 只能靠扫参不能靠学习；**Stage 0 的
离线测量基本就是最终答案**，不存在"训练一下会变好"的余地。这一点应该在论文里主动
讲清楚，否则会被审稿人当成缺陷追问。

### H. `λ·log w` 的标定分布变了（附带一个好消息）

现在同一层的槽宽度相同，`log w` 只区分距离；语义簇下 `w` 完全由内容决定，分布高度
偏斜（一个主导簇 w=20000，十几个小簇 w=1~10）。`λ` 是隐含地按旧分布 CPT 出来的。

**好消息**：mass bias 在"槽内 token 完全相同"时精确、否则一阶——而语义聚类正好在
最小化这个误差项（Jensen 间隙随簇内同质性收缩）。所以 **bias 的精度实际上是提升的**，
这是个可以写进论文的、之前没注意到的副收益。

### I. 逐层差异

早期层的 key 接近 token embedding 加位置，几乎没有主题结构。§2.5 那条"必须逐头
报告"应扩展成**逐层 × 逐头**。很可能的结果是这套方法在中层的语义头上有效、在浅层
和位置头上是 no-op——这决定了是否该做成"只对部分层/头启用"。

## 12. 变更记录

> 每次讨论产生突破或进展,在这里加一条,新的在最上面。只记"改变了什么结论/设计",
> 不重复已经写进正文的细节——细节改到对应章节,这里留指针和一句话动机。

- **2026-08-14｜写入完整算法规格（新 §5.1–§5.12），新增 §11 技术难点清单，修掉
  三处欠账。** 动机：用户要求把"到底怎么分簇、簇内怎么压缩保证 O(log n)"落成可实现
  的规格。要点：①**度量统一为原始 pre-RoPE key 上的平方欧氏距离**，因为同一个量
  同时是 DP-means 分配准则、Ward 合并代价、和 rank-1 残差，一个度量贯穿三处；且
  阈值必须相对化 `λ_new = λ_rel·s_h`，否则跨层不可比、Stage 0 结论无意义。
  ②**三路判定**：统一代价 `‖k−μ‖² + η·φ(gap)` 只用于挑候选，两个阈值分别管"新簇"
  和"新段"——揉成一个阈值就是重犯 v3 的错。③**三阶段批量化路由**解决 §11-B 的
  wall-clock 问题，关键摊还论证是"串行的 Phase 2 总次数被 E[K]≈α ln n 界住而非
  O(T)"。④**O(log n) 的准确陈述**：`Σ_c B′log(n_c/B′) ≤ K·B′·log(n/(K·B′))`，由 log
  的凹性可知**最坏情况是簇均衡**，界是安全的。⑤**"小簇无损"**（成员数 ≤ B′ 的簇
  永不合并）被识别为 §3 needle 论证的真正承重点，依赖 level 0 存单 token 这个改动。
  三处欠账：**记号冲突**（`λ` 曾同时表示 mass bias 和 segment 阈值）已统一为
  `λ_new`/`η`/`g_max`，`λ` 只留给 mass bias；**`SEG_max` 预分配爆炸**（曾写成 4 万槽）
  由 §5.11"每 cluster 一条 ladder，segment 只约束合并"修掉；**§4 两笔内存账**
  （持久 cache vs 读出瞬时槽池）分开列，锚点展开的 M 只影响后者。
  另新增 S0.8（批量化近似的分歧率）并给 S0.4 加上 value 方差（§11-D）。
- **2026-08-14｜v3 → v3.1：时序打断不再 fork 新簇，改为 cluster/segment 两层分离。**
  动机：用户要求为 v3 的"语义相近但位置不同就分开"找反例。找到三个，反例一致命——
  高频复现实体（"Alice"被提 50 次）在 fork 规则下产生 50 个语义相同的簇，K 正比于
  话题切换次数而非 log n，且"内容越重要越会复现于是越贵"的激励方向是反的。根因是
  fork 把**簇身份**与**存储局部性**绑死。解法：cluster 只管语义身份，segment 只管
  时序局部性。副产品：最早被降级的"连续性约束 Ward 合并"作为存储层回归，两条线合流。
  连带产出：①`η` 是连接两套方案的连续旋钮，扫它等于回答"收益来自语义分组还是分段
  边界"，提升为 **S0.0 最高优先级**；②反例二写入 §2.4——segment 只解决远距离更正，
  **相邻更正仍被均值池化**；③反例三写成 §2.5——**聚类发生在 attention key 空间而非
  语义空间**，因此 Stage 0 所有统计**必须逐层逐头报告**。
- **2026-08-14｜v2 → v3：位置表示从"求平均的 z 统计量"改为"不做平均的真实锚点集"。**
  动机：用户指出 v2 的 z 仍然是加权求和，相位相消依然存在，只是被诚实报告而已。
  解法：放弃合成任何"平均位置"，只存真实成员的 `(p_lo, p_hi, p_mid)`，读出时在每个
  真实位置上做标准 `apply_rope`（ρ≡1，相消不可能发生）；合并用 min/max，幂等半格
  运算比加权圆均值更贴合 Fenwick 结构。用户同时提出的"语义相近但被打断就该新开"，
  被 DeltaKV 实测的"60% 相似 token 相距 >16 位置"证明是**前提而非补丁**。
  连带产出：①mass bias 必须按去重锚点数 `M_s` 摊薄，否则大跨度簇权重被静默放大
  M 倍；②v2 的 `κ`/`log ρ` 置信项整条被消解；③新增 S0.6、S0.7。
- **2026-08-14｜新增 §2.4 开放问题：均值池化无法表达 supersession。** 动机：用户提出
  "考 100 分→更正为 90 分"场景并提议每簇维护 gated DeltaNet 线性 state。结论：
  **问题真实，但该方案暂不采纳**——线性 state 的 `qᵀS` 读出没有选择性，恰是捞针最弱
  的机制，且簇内 key 近平行是线性容量最坏情况，会让阈值失手的代价从"稀释"恶化成
  "不可恢复的抹除"；另外 KV cache 不该替模型做不可逆的遗忘决定（用户认可此点）。
  可行替代路径：现有 rank-1 Γ **结构上已是秩 1 线性 state**，改构造方式即可。
  分歧交由 S0.7 判定。
- **2026-08-14｜v1 → v2：位置重物化从 β 插值旋钮改为满模长旋转 + log ρ 独立置信项。**
  （已被 v3 取代，保留作为迭代记录）动机：v1 的 `gain=ρ^{(β-1)}` 在 β=0 时对最不
  可信的频率通道施加最大重物化力度，是方向性错误。
- **2026-08-14｜方案定型：语义簇 + 簇级抽象位置，取代最初的"连续性约束 Ward 合并"。**
  Ward 合并降级为簇内合并顺序策略。**注：v3.1 把它作为 segment 存储层重新纳入。**
- **2026-08-14｜建档。** 从 `claude/semantic-cluster-log-compression-8ai32a` 分支切出
  `semanticLogKV`，本文件取代该分支的 CLAUDE.md 作为独立存档。
